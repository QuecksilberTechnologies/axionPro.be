# Attendance punch flow — 2026-09-24

## Environment and scope

- Source/build environment: Windows local workspace, .NET 10, 2026-09-24.
- Scope: authenticated Mobile/Web self punch, today status, typed policy/location enforcement, idempotency and ordering.
- Database status: the three attendance/work-arrangement scripts were applied successfully to the configured Development database. API code remains local until deployed.

## Scenarios

| Scenario | Expected | Actual |
|---|---|---|
| First CheckIn | accepted | automated rule passed |
| CheckOut without CheckIn | rejected | automated rule passed |
| Repeated CheckIn | rejected | automated rule passed |
| CheckIn then CheckOut | accepted | automated rule passed |
| Repeated CheckOut | rejected | automated rule passed |
| Token identity only | request cannot choose employee | source contract passed |
| Duplicate network retry | unique idempotency key returns original punch | source/schema contract passed |
| Concurrent requests | employee transaction lock serializes writes | source contract passed |
| Device type/policy/location/geofence checks | inactive, unsupported, or policy-disabled sources and invalid locations are rejected before insert | source contract passed |
| Today timeline | ordered punches and current state returned | endpoint/source contract passed |

## Commands and results

`dotnet build AxionPro.sln --no-restore` completed with 0 errors. Existing repository warnings remain.

`dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~AttendancePunchFlowTests|FullyQualifiedName~EmployeeWorkLocationArrangementValidationTests" --no-restore --verbosity minimal`

Focused result: **21 passed, 0 failed, 0 skipped**.

The full suite was run while the local API host was listening. Result: **541 passed, 0 failed, 95 skipped**. The skipped cases require optional database/environment credentials or feature flags and were not reported as passes.

Unauthenticated local HTTP probes returned `401` for both `GET /api/Attendance/today` and `POST /api/Attendance/mark-attendance`, confirming bearer authentication is enforced.

## Persistence and cleanup

The configured Development database successfully executed:

- `database-scripts/AlignEmployeeWorkLocationAndArrangementValidation.sql`
- `database-scripts/AddAttendancePolicyVersionConfiguration.sql`
- `database-scripts/AddEmployeeAttendancePunch.sql`

A qualifying real employee/arrangement/policy/location tuple was selected without logging its identity. Inside a database transaction, one real-shaped attendance punch inserted successfully, the same idempotency key was rejected by the unique database constraint, and the transaction was rolled back. No smoke-test attendance row was retained.

The follow-up device-type migration also ran successfully on the configured Development database. It seeded exactly four active codes (`MOBILE`, `WEB`, `BIOMETRIC`, `MANUAL`), retained `IsDeviceRegister=true` only for `BIOMETRIC`, added the punch foreign-key column, and removed the temporary numeric `Channel` column. The focused attendance suite then passed **8 of 8** tests.

## Unresolved live checks

An authenticated employee success-path HTTP check still requires an actual Tenant employee bearer token; none was stored in the test environment. Therefore authenticated HTTP persistence is **not run**, while real database persistence/rollback and unauthenticated HTTP enforcement are verified. Biometric device ingestion remains a separate trusted-device flow and is not handled by the Mobile/Web endpoint.
