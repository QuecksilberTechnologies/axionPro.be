# Subscription billing foundation — 2026-09-24

## Scope and status

- Scope: India-first gateway-neutral billing schema, Cashfree metadata, centralized retry/grace configuration and implementation handoff.
- Status: schema and HostAdmin configuration foundation implemented. Provider/worker and sandbox acceptance are pending, so payment processing is not release-ready.

## Decisions

- Cashfree first adapter; INR and India GST launch.
- Monthly/yearly Tenant choice; immediate prorated upgrade and next-cycle downgrade.
- Host-admin partial/full refund.
- Three application retries and one-day grace, both configuration-bound.
- Webhook/reconciliation confirmation is authoritative; browser redirects cannot grant entitlement.

## Validation

The configured Render database migration completed and all 15 expected billing tables were verified. All four explicit Host-user audit columns on plan prices and tax rules were verified. The expanded Host billing seed passed on its first run and idempotent rerun; verification returned seven modules, 16 exact operation mappings and 16 Host-Super-Admin grants. Repository-level read smoke passed for plan prices, tax rules, transactions, refunds, reconciliation and audit after correcting typed nullable PostgreSQL filters. The database currently contains one tax rule and no plan-price/payment/refund/audit business rows. The API project build completed with 0 errors. Focused contracts passed: 6 passed, 0 failed, 0 skipped.

Authenticated HTTP execution was not performed because no Host bearer token was placed in the test environment. This is recorded as not run, not as a pass.

## Pending acceptance

Cashfree merchant onboarding, seller GST identity, sandbox credentials, provider checkout/refund execution, webhook processor, worker, invoices and authenticated sandbox smoke tests remain pending. Host configuration, pricing, tax, transaction/refund/audit views, validated refund queueing, reconciliation summary and webhook retry queueing endpoints are implemented.
