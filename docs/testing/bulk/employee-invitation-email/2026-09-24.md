# Employee bulk invitation email smoke — 2026-09-24

## Scope and status

- Scenario: verify whether Employee bulk import sends welcome email and whether Tenant 8 is ready to deliver it.
- Result: application contracts PASS; configured database readiness PASS; live external delivery NOT RUN.
- Bulk import itself does not send an email. The authenticated `bulk/send-invitations` action claims successfully imported Employee rows and sends the welcome email separately.

## Environment and prerequisites

- Local source/build on 2026-09-24.
- Configured Development and Production settings point to the same Render PostgreSQL database.
- No recipient address or approved test mailbox was supplied, so no external email was sent.
- No SMTP credentials, tokens or employee identity values are included in this report.

## Expected and actual results

| Check | Expected | Actual | Status |
| --- | --- | --- | --- |
| Bulk insertion | Persist Employee without coupling transaction success to SMTP | Invitation dispatch is a separate action | PASS (code inspection) |
| Invitation claim | Claim eligible rows once and persist delivery status | Covered by workflow contracts | PASS (automated) |
| Template selection | Tenant active template, otherwise same-code default template | Runtime resolver follows this order | PASS (automated) |
| Tenant 8 SMTP readiness | One active Tenant configuration | 1 active row | PASS (configured DB observation) |
| Tenant 8 welcome template | One active `WELCOME_EMAIL` | 1 active row | PASS (configured DB observation) |
| Default fallback readiness | Active default SMTP and `WELCOME_EMAIL` | 1 SMTP and 1 template row | PASS (configured DB observation) |
| Real recipient receives message | SMTP accepts and mailbox receives welcome email | Not executed without an approved test recipient | NOT RUN |

## Automated verification

Command:

`dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --no-restore --filter "TestCategory=EmployeeImportInvitations|FullyQualifiedName~EmployeeImportDatabaseTests.Invitations|Name~Email_service_resolves_tenant_template" --nologo --verbosity:minimal`

Result: 8 passed, 0 failed, 1 skipped. The skipped invitation database test requires its isolated database fixture; it is not counted as a pass.

## Persistence and cleanup

Read-only database counts were used for readiness. No Employee, invitation, queue or email records were inserted, and no cleanup was required.

## Remaining acceptance

Run `bulk/send-invitations` for a disposable imported Employee whose official email is an approved test mailbox, then verify the action response, persisted invitation status, SMTP acceptance log and mailbox receipt. Until that succeeds, actual delivery remains unproven.
