# Bulk: existing-module operation cleanup — 2026-09-13

## Scope and status

Remove all separate Tenant and Host bulk navigation modules. Keep bulk behavior
available through Import and Export operations on existing functional modules.

**Overall status: PASS for seed execution and target DB reconciliation.** API/UI
menu smoke calls were not executed in this scenario.

- Branch: `main_branch`; changes were uncommitted during the recorded run.
- Isolated DB: `127.0.0.1:55439/axionpro_bulk_test`, restored from the existing
  workforce backup into a temporary trust-authenticated local PostgreSQL cluster.
- Target: Render development PostgreSQL from the API development configuration.
- Target backup: `artifacts/db-backups/before-bulk-module-cleanup-20260913-141348.dump`
  (821,718 bytes).
- Credentials and connection secrets are excluded.

## Expected functional mapping

| Feature | Existing ModuleCode | Operations |
| --- | --- | --- |
| Employee | `EMP_LIST` | Import, Export |
| Department | `TENANT_DEPARTMENTS` | Import, Export |
| Designation | `TENANT_DESIGNATIONS` | Import, Export |
| Role | `TENANT_ROLES_PERMISSIONS` | Import, Export |
| EmployeeType | `TENANT_EMPLOYEE_TYPES` | Import, Export |
| Host Card | `HOST_TENANT_RFID_MANAGEMENT` | Import, Export |
| Host Device | `HOST_DEVICE_SETUP` | Import, Export |
| Host Module | `HOST_MODULES` | Import, Export |
| Host Child Module | `HOST_SUBMODULES` | Import, Export |
| Host Operation | `HOST_OPERATIONS` | Import, Export |
| Host Module Operation | `HOST_MODULE_OPERATIONS` | Import, Export |

Operation types are Import=12 and Export=11. Numeric IDs remain database-specific
and UI consumers must discover them from the authenticated menu/permission flow.
Existing Module PageName and Operation OperationName values were not rewritten.

## Execution and results

| Case | Action | Expected | Actual | Status |
| --- | --- | --- | --- | --- |
| SEED-01 | Run complete seed on isolated restored DB | FK-safe completion | Exit 0 | PASS |
| SEED-02 | Rerun the same complete seed | Idempotent completion | Exit 0 | PASS |
| TEST-01 | Run seed and Host permission/import focused tests | No regression | 36 passed, 0 failed, 0 skipped | PASS |
| TARGET-01 | Fresh target `pg_dump` | Restorable backup artifact | 821,718-byte custom dump created | PASS |
| TARGET-02 | Run targeted Tenant and Host seeds | Complete transactions | Both completed successfully | PASS |
| TARGET-03 | Reconcile Module catalogue | No bulk navigation modules | 0 rows where ModuleCode contains `BULK` | PASS |
| TARGET-04 | Reconcile tenant entitlement integrity | No orphan rows | TenantEnabledModule 0; TenantEnabledOperation 0 | PASS |
| TARGET-05 | Reconcile functional operation mappings | 2 per 11 modules | 22 active mappings; 0 duplicate pairs | PASS |
| TARGET-06 | Verify restored tenant hierarchy | Functional parents restored | EMP_LIST→EMP_MGMT; four tenant masters→TENANT_MGMT | PASS |

## Change behavior and persistence

The targeted scripts migrate matching persisted role/tenant-operation grants from
obsolete bulk modules to their corresponding functional module when that grant
does not already exist. They then delete obsolete references from
`HostRoleModuleAndPermission`, `RoleModuleAndPermission`,
`TenantEnabledOperation`, `TenantEnabledModule`, `PlanModuleMapping` and
`ModuleOperationMapping` before deleting obsolete Module rows.

The legacy `BULKUPLOAD` parent had five real functional modules attached in the
target database. The tenant seed restored `EMP_LIST` under `EMP_MGMT` and
Department, Designation, Role and EmployeeType under `TENANT_MGMT` before deleting
the obsolete parent. No existing functional Module PageName or OperationName was
changed.

## Automated verification

```powershell
$env:AXIONPRO_BULK_TEST_CONNECTION='Host=127.0.0.1;Port=55439;Database=axionpro_bulk_test;Username=postgres'
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter 'Category=BulkModuleOperationSeed|Category=HostBulkPermission|Category=HostBulkImport'
```

Result: **36 passed, 0 failed, 0 skipped**. The added database regression checks
that obsolete modules are absent, all eleven functional modules own exactly one
active Import and Export mapping, and no Module/Operation mapping pair is duplicated.
Evidence: `artifacts/bulk-module-operation-tests.log`,
`artifacts/bulk-module-seed-first-run.log`, and
`artifacts/bulk-module-seed-rerun.log`.

An earlier HostBulkDatabase run failed because the old restored fixture lacked
newer Host bulk tables and also already contained grants on the now-shared modules.
It is not recorded as a pass and is outside the final focused 36-test result.

## Remaining acceptance

1. Deploy the constants change with the API build so Host bulk endpoint module-code
   validation expects the existing functional modules.
2. After deployment, authenticate as Tenant and Host users, load `my-menu`, and
   confirm Import/Export appear on the existing modules according to role grants.
3. Run one authorized Tenant bulk and one authorized Host bulk preview/job smoke
   test using dynamically discovered IDs. Record those in a new run section.
