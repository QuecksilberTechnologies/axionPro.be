# Database: manual table removal reconciliation — 2026-09-16

## Scope and status

- Scenario ID: `DB-MANUAL-REMOVAL-RECONCILIATION-20260916`
- Acceptance: reconcile the API EF model with tables manually removed from the
  production database and confirm that the API can build and start.
- Overall status: PASS for EF reconciliation, build and startup. Endpoint source
  retirement remains separate work.
- Environment: local API connected to the Render PostgreSQL database.

## Results

| Case | Expected | Actual | Status |
| --- | --- | --- | --- |
| Live schema inventory | Read current schema without mutation | 128 tables/views found | PASS |
| Pre-fix EF comparison | Identify every mapped missing object | 22 missing mappings found | PASS |
| Post-fix EF comparison | No EF table mapping may target a missing object | 127 mapped tables, 0 missing; DB-only `License` remains | PASS |
| Solution build | No compiler errors | Build succeeded, 0 errors | PASS |
| API startup | Host starts and active background queries reach DB | `AXIONPRO APPLICATION STARTED SUCCESSFULLY`; DeviceCommand and BulkImportJob queries succeeded | PASS |

The 22 excluded legacy objects are recorded in
`DeletedTableModelQuarantineTests.cs`. They include the removed policy,
insurance, leave-policy/sandwich, candidate, workflow and legacy catalogue
objects. The EF exclusions prevent startup/model access from targeting relations
that no longer exist.

## Automated verification

- Category: `DeletedTableModelQuarantine`
- Expected count: 1 test.
- Full solution build and live startup smoke are separate commands.

## Limitation

The old controllers, handlers and repositories are retained temporarily because
several share contracts with active attendance, employee and tenant-creation
flows. Calling a retired feature endpoint is unsupported. Removing those source
slices safely requires a separate endpoint-retirement pass; this reconciliation
does not describe those endpoints as operational.
