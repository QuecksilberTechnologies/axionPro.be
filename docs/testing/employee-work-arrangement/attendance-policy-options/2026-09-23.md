# Employee Work Arrangement: Attendance policy options — 2026-09-23

## Scope and status

- Scenario ID: `EWA-ATTENDANCE-POLICY-DDL-001`
- User-requested behavior / acceptance criteria: token-authenticated, permission-ID-free Attendance policy dropdown; stable category-code discovery; automatic current-date fallback; Published/effective date filtering; clear empty state; focused tests.
- Overall status: PASS locally for build, contract tests and unauthenticated HTTP; authenticated data behavior and deployment remain PENDING.
- Environment and API base URL: local Development, `http://127.0.0.1:7788` for the unauthenticated smoke call.
- Branch, code commit and independently verified deployed build: working tree; no commit/deployment asserted.
- Run time and timezone: 2026-09-23, Asia/Calcutta.
- UI handoff: [Employee Work Arrangement Attendance Policy Options](../../../UIdeveloperDoc/EMPLOYEE_WORK_ARRANGEMENT_ATTENDANCE_POLICY_OPTIONS.md).

## Prerequisites and test data

The data-result scenarios require a valid Tenant bearer token plus active generic Policy records connected through an active `ATTENDANCE` category, active/non-deleted Policy Type and Policy, and an active Published Policy Version whose effective window contains the requested date. No credentials or records were created for this run.

Illustrative request:

```http
GET /api/EmployeeWorkArrangement/attendance-policy-options?effectiveOn=2026-09-23&search=hybrid
Authorization: Bearer <redacted>
```

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| EWA-DDL-01 | Build `axionpro.api` | Compile with no errors | 0 errors; existing warnings reported | Local | PASS | Build command below |
| EWA-DDL-02 | Run focused contract suite | Route/auth/DTO/filter/empty-state contracts pass | 4 passed, 0 failed, 0 skipped | Local | PASS | Test command below |
| EWA-DDL-03 | GET endpoint without bearer token | Standard 401 before handler/repository data access | HTTP 401, `UNAUTHORIZED` | Local HTTP | PASS | Sanitized response below |
| EWA-DDL-03A | Employee Work Arrangement + Policy Framework regression categories | Existing relevant contracts remain green | 23 passed, 0 failed, 0 skipped | Local | PASS | Regression command below |
| EWA-DDL-04 | GET with authenticated Tenant and effective policy fixtures | Correct tenant-isolated options and date/version selection | Not run; no credential/fixture used | Local | PENDING | Requires authorized token and controlled data |
| EWA-DDL-05 | Deployed endpoint | Same contract on deployed build | Not run | Deployed | PENDING | Requires deployment |

Sanitized observed response for EWA-DDL-03:

```json
{"IsSucceeded":false,"Message":"The request is not authenticated.","Data":null,"Errors":[],"ErrorCode":"UNAUTHORIZED"}
```

## Investigation and changes

- Confirmed cause of the empty UI dropdown: Work Arrangement still used legacy `AttendancePolicyId`; no generic Attendance-policy option endpoint existed.
- Added a token-only dropdown endpoint and repository query based on stable category/status codes and effective-date selection. The date is optional for the caller and defaults to today's UTC date; UI sends Work Arrangement Effective From automatically when available.
- The generic dropdown response intentionally includes exact `PolicyVersionId`.
- Existing legacy Work Arrangement create/update persistence was not silently migrated; that breaking schema/contract change remains explicit in the UI handoff.

## Automated verification

- Build command: `dotnet build axionpro.api\axionpro.api.csproj --no-restore`
- Build result: PASS, 0 errors; 583 pre-existing/project-wide warnings were reported.
- Test command: `dotnet test axionpro.automationtests\axionpro.automationtests.csproj --no-restore --filter "FullyQualifiedName~EmployeeWorkArrangementAttendancePolicyOptionsTests"`
- Tests: 4 passed / 0 failed / 0 skipped / 4 total.
- Regression command: `dotnet test axionpro.automationtests\axionpro.automationtests.csproj --no-build --filter "Category=EmployeeWorkArrangement|Category=PolicyFramework"`
- Regression result: 23 passed / 0 failed / 0 skipped / 23 total.
- Tests establish controller authorization, absence of permission-ID fields, route/DTO response contract, repository filter contract, mandatory date and actionable empty-state handler behavior.
- Tests do not establish authenticated database results or deployed behavior.

## Persistence and cleanup

The endpoint is read-only. No application data was inserted, updated, deleted, uploaded or retained. The local API process used for the 401 check was stopped after the request.

## Remaining acceptance and completion criteria

- Execute an authenticated controlled-data matrix covering another tenant, inactive/deleted masters, Draft/Archived versions, expired/future windows, search and overlapping Published versions.
- Deploy the updated build and repeat route/authentication and data-result checks.
- Decide and separately migrate the legacy Work Arrangement `AttendancePolicyId` contract if the selected generic `PolicyVersionId` must be persisted by Create/Update.
