# Employee Work Arrangement: Attendance policy permission-pipeline regression — 2026-09-24

## Scope and status

- Scenario ID: `EWA-ATTENDANCE-POLICY-DDL-002`
- Trigger: deployed authenticated request returned `FORBIDDEN` although the endpoint contract is token-only.
- Root cause: `GetAttendancePolicyOptionsQuery` is in the `EmployeeCmd` namespace, so `EmployeeTenantPermissionBehavior` rejected it for missing `ModuleId` and `OperationId` before its handler executed.
- Resolution: after successful tenant-token validation, the permission behavior now allows only this exact dropdown query to continue without a module-operation lookup. All other Employee requests retain the established permission pipeline.
- Overall status: PASS locally for compilation and focused pipeline/contract tests. Corrected deployed behavior remains PENDING until this build is deployed.

## Steps and results

| Case | Action | Expected | Actual | Status |
| --- | --- | --- | --- | --- |
| EWA-DDL-06 | Reproduce from supplied deployed response | Authenticated request should reach handler | Deployed response was `403 FORBIDDEN` | FAIL (observed deployed build) |
| EWA-DDL-07 | Execute behavior with valid Tenant context and no permission IDs | Validate token, skip permission persistence, invoke next handler | Token validation called once; no permission repository call; next invoked | PASS |
| EWA-DDL-08 | Execute behavior with failed Tenant authentication | Reject before handler | `UnauthorizedAccessException`; next not invoked | PASS |
| EWA-DDL-09 | Run full `EmployeeWorkArrangement` focused category | All endpoint, filter and pipeline contracts pass | 6 passed, 0 failed, 0 skipped | PASS |
| EWA-DDL-10 | Repeat request on deployed corrected build | HTTP 200 with options or documented empty state | Not run; corrected build is not deployed from this task | PENDING |

## Automated verification

```powershell
dotnet test axionpro.automationtests\axionpro.automationtests.csproj --no-restore --filter "Category=EmployeeWorkArrangement" --logger "console;verbosity=minimal"
```

Result: 6 passed / 0 failed / 0 skipped. Existing dependency warnings were emitted; compilation produced no error.

## Persistence and cleanup

No database rows were read or changed by the new permission-behavior tests. No credential or bearer token was stored.

## Remaining acceptance

- Deploy the corrected build.
- Repeat the authenticated deployed request and verify HTTP 200.
- Verify both a populated result and the documented no-published-policy empty result with controlled Tenant data.
