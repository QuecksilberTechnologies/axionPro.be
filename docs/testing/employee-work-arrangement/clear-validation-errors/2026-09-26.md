# Employee Work Arrangement clear validation errors — 2026-09-26

## Environment and build

- Local source branch: `main_branch`.
- API project: `axionpro.api` targeting .NET 10.
- `dotnet build axionpro.api/axionpro.api.csproj --no-restore`: PASS, 0 errors; existing repository warnings remain.

## Scenario

Work Arrangement create, update, and activation must explain the exact employee, tenant-location,
employee-location-assignment, location-type, or effective-date condition that prevents persistence.
Soft-deleted rows must never satisfy any validation dependency.

Reported example:

- Employee: Sumit Verma (sanitized business reference).
- Arrangement start: `2026-09-26`.
- Selected employee-location assignment start: `2027-02-04`.
- Expected: rejection naming both dates and the two valid corrections.
- Code result: the late-start diagnostic formats `04/02/2027` and `26/09/2026` in the response message.

## Expected and implemented behavior

The validator distinguishes these conditions in order: employee missing/deleted, employee inactive,
location missing/deleted, location inactive, incompatible location type, assignment missing, assignment
inactive, assignment not primary, attendance disabled, late start, early end, and an open-ended arrangement
covered only by a finite assignment.

Repository diagnostic queries include `!x.IsSoftDeleted`. Therefore a soft-deleted employee, location,
or assignment is ignored even if `IsActive = true`; it cannot cover an arrangement.

## Automated verification

Command:

```powershell
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --no-restore --filter "EmployeeWorkArrangement|EmployeeWorkLocation" --logger "console;verbosity=minimal"
```

Result: PASS — 29 passed, 0 failed, 0 skipped.

Coverage includes location-type compatibility, inclusive date overlap, complete assignment coverage,
attendance policy compatibility, repository soft-delete predicates, dependency protection, and presence
of the detailed status/date diagnostic paths.

Full-suite command:

```powershell
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --no-restore --logger "console;verbosity=minimal"
```

Initial result: 541 passed, 3 failed, 95 skipped. All three failures were HTTP connection
refusals because the API host was not running on `localhost:5170`; they were not assertion failures.
After starting the local API, the exact three failed tests were rerun and passed 3/3:

- anonymous protected-navigation rejection;
- client-device detection;
- Swagger document availability.

Skipped tests remain recorded as skipped and are not counted as passes.

## Persistence and cleanup

- No database schema or seed change was required.
- No live data was inserted or modified by this run.
- No cleanup was required.

## Verification limits

- Authenticated HTTP reproduction against the user's running tenant: NOT RUN.
- Deployed verification: NOT RUN.
- This report records local build and automated behavior checks only.
