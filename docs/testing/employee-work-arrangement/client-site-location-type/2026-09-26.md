# Employee Work Arrangement: Client Site location type — 2026-09-26

## Scope and status

- Scenario ID: EWA-CLIENT-SITE-TYPE-20260926
- User-requested behavior / acceptance criteria: find and fix the repeated Client Site arrangement
  validation failure, correct confirmed development data, prevent invalid selections, test, and commit.
- Overall status: PASS for source contract, focused tests, production build and database prerequisites;
  authenticated HTTP create was not run.
- Environment and API base URL: local Angular source and configured Development PostgreSQL database.
- Branch, code commit and independently verified deployed build: UI `development`; deployed build not
  independently verified.
- Run time and timezone: 2026-09-26, Asia/Calcutta.
- Related UI handoff: [Employee Work Location and Work Arrangement validation](../../../UIdeveloperDoc/EMPLOYEE_WORK_LOCATION_ARRANGEMENT_VALIDATION.md).

## Prerequisites and test data

The reported tenant, employee, policy and locations were inspected through the configured Development
connection without recording credentials. Evidence is sanitized to tenant/location/employee labels
already supplied by the user. No token or identity document was retained.

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| ENUM-01 | Compare Angular and backend `TenantLocationType` | Numeric contracts match | Angular omitted Office and encoded Client Site as 5; backend Client Site is 6 | local source | FAIL before fix | source comparison |
| DATA-01 | Read reported TenantLocation rows | Client-site rows store type 6 | SAMDARIYA MALL stored 5; Railway-Client stored 1 | Development DB | FAIL before fix | direct read |
| DATA-02 | Guarded correction of the two confirmed rows | Both become type 6 and are not head-office rows | exactly 2 rows updated and committed | Development DB | PASS | update `RETURNING` output |
| DATA-03 | Inspect Sumit/Railway assignment | Active, attendance-allowed, primary and date-covering | primary was false | Development DB | FAIL before fix | direct read |
| DATA-04 | Guarded assignment correction | Exact assignment becomes primary | exactly 1 row updated and committed | Development DB | PASS | update `RETURNING` output |
| DATA-05 | Reconcile reported 2028-02-04 prerequisites | Published policy/config, Client Site type, primary covering assignment, no active arrangement | all conditions matched | Development DB | PASS | joined reconciliation query |
| UI-01 | Focused Angular tests | all pass | 38 passed, 0 failed, 0 skipped | local | PASS | command below |
| UI-02 | Production Angular build | build succeeds | succeeded with existing budget warnings | local | PASS | command below |
| HTTP-01 | Authenticated create through running UI/API | arrangement persists | not run; browser token/session was unavailable to automation | live UI | NOT RUN | no HTTP evidence captured |

## Investigation and changes

- Observed failure: API returned “The selected primary location type is not valid for the selected work mode.”
- Confirmed cause and evidence: Angular's enum missed `Office = 3`, so later values were shifted. Existing
  reported rows also contained incorrect persisted types, and the selected employee assignment was not primary.
- Suspected cause / remaining uncertainty: the incorrect Railway value `1` likely came from a prior form/default;
  no claim is made about the exact historical request payload because it was not captured.
- Changed files and commit: Angular enum/options were committed separately; this run adds compatible-location
  filtering, a stale-selection guard, Client Site required-location validation and tests.
- Regression tests establish enum compatibility and relevant form/store behavior. They do not establish a
  deployed authenticated POST.

## Automated verification

- Command: `npx ng test --watch=false --include="src/app/shared/utilities/common-data.spec.ts" --include="src/app/features/tenant-locations/**/*.spec.ts" --include="src/app/features/user-menu/employee-profile/employee-work-arrangement-info/**/*.spec.ts"`
- Counts: 38 passed / 0 failed / 0 skipped / 38 total.
- Build: `npm run build` passed; existing initial-bundle and form-editor CSS budget warnings remain.

## Persistence and cleanup

- `TenantLocation` tenant 10 rows 8 and 9 were guarded by IDs, codes, prior types and non-deleted state,
  then changed to `LocationType = 6`, `IsHeadOffice = false` with updated timestamps.
- `EmployeeLocationAssignment` employee 16/location 9 was guarded by tenant, employee, location, active,
  non-deleted and non-primary state, then changed to primary with an updated timestamp.
- Each transaction required the exact expected row count and would roll back otherwise. Records were retained
  because they are the user's intended configuration.

## Remaining acceptance and completion criteria

- Reload/restart the local Angular development server so it serves the new bundle, then perform one authenticated
  create and confirm the new `EmployeeWorkArrangement` row/API response. This was not recorded as a pass.
