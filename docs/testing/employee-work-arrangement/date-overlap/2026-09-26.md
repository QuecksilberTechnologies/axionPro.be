# Employee Work Arrangement: date overlap — 2026-09-26

## Scope and status

- Scenario ID: EWA-DATE-OVERLAP-2026-09-26
- User-requested behavior / acceptance criteria: allow multiple dated Work Arrangements for one employee while rejecting overlapping active periods with a clear explanation.
- Overall status: PASS for local build, focused logic/contract tests and HTTP host smoke; authenticated mutation and deployed acceptance remain pending.
- Environment and API base URL: local source and automated test environment; authenticated API base URL not used.
- Branch, code commit and independently verified deployed build: `main_branch`; commit pending; deployed build not verified.
- Run time and timezone: 2026-09-26, Asia/Calcutta.
- Prior report / related UI handoff: [location consistency](../location-consistency/2026-09-24.md), [UI handoff](../../../UIdeveloperDoc/EMPLOYEE_WORK_LOCATION_ARRANGEMENT_VALIDATION.md).

## Prerequisites and test data

No real employee data or credentials are required for the pure effective-window cases. Dates are
sanitized test values. Repository contract checks inspect the existing tenant, active-state and
soft-delete predicates without mutating a database.

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| EWA-01 | Existing 01/01–31/01; requested starts 31/01 | Reject because date boundaries are inclusive | Overlap helper returned true | Local automated | PASS | `Inclusive_effective_windows_detect_overlap_and_allow_adjacent_history` |
| EWA-02 | Existing 01/01–31/01; requested starts 01/02 | Allow as non-overlapping history | Overlap helper returned false | Local automated | PASS | `Effective_window_overlap_covers_past_boundary_future_and_open_ended_cases` |
| EWA-03 | Either active period has no end date | Reject every later intersecting active period | Parameterized open-ended cases returned true | Local automated | PASS | Same parameterized test |
| EWA-04 | Create, update or activate | Use the shared overlap validator and exclude the edited row | Source contract confirms all three handler paths call `ValidateNoDateOverlapAsync` | Local automated | PASS | `Repository_and_handlers_enforce_cross_record_and_date_window_rules` |
| EWA-05 | Soft-deleted or inactive existing row | Do not count it as an active conflicting arrangement | Repository query requires `IsActive` and excludes `IsSoftDeleted` | Local automated | PASS | Repository source-contract assertions |
| EWA-06 | API-dependent regression smoke | Navigation rejects anonymous, client detection responds, Swagger is available | Initial full-suite run failed 3/3 with `ECONNREFUSED`; after starting the local API, rerun passed 3/3 | Local HTTP | PASS | Commands below |

No authenticated HTTP response was captured.

## Investigation and changes

- Observed failure: the overlap rule existed, but create, update and activation repeated a boolean repository call and returned only a generic conflict message.
- Confirmed cause and evidence: three handler paths independently called `CurrentArrangementExistsAsync`; the repository discarded the conflicting date range.
- Suspected cause / remaining uncertainty: none for the code path; authenticated concurrent-write behavior is not exercised locally.
- Changed files and commit: application validator, repository contract/implementation, constants, automated tests, UI handoff and this report; commit pending.
- What the executed regression tests establish and do not establish: overlap boundaries, shared handler wiring, repository filtering, build compatibility and local API availability pass. They do not establish authenticated tenant mutation or deployment.

## Automated verification

- Focused command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~EmployeeWorkLocationArrangementValidationTests" --no-restore --verbosity minimal`.
- Focused counts: 19 passed, 0 failed, 0 skipped.
- Full command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --no-restore --verbosity minimal`.
- Full counts with API initially stopped: 547 passed, 3 failed, 95 skipped, 645 total. All three failures were `ECONNREFUSED ::1:5170`; they are recorded as observed failures, not passes.
- HTTP rerun after starting the local API: 3 passed, 0 failed, 0 skipped. Tests were `Protected_navigation_endpoint_rejects_an_anonymous_request`, `Detect_device_returns_the_automation_client_context`, and `Swagger_document_is_available_and_contains_routes`.
- The 95 skipped tests require configured database/authenticated/host-device or other explicit integration prerequisites; they remain skips.
- Build result: PASS with existing compiler and NuGet advisory warnings, 0 errors.

## Persistence and cleanup

No database records are created by these focused tests. The existing PostgreSQL exclusion constraint
continues to protect active, non-soft-deleted Work Arrangement periods from concurrent overlaps.

## Remaining acceptance and completion criteria

- Authenticated local or deployed create/update/activation acceptance remains separate unless executed.
