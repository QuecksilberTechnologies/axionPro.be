# Employee Work Arrangement: location consistency — 2026-09-24

## Scope and status

- Scenario ID: EWA-LOCATION-CONSISTENCY-2026-09-24
- User-requested behavior / acceptance criteria: Cross-check arrangement primary location, work mode/location type, effective windows, overlapping active records, and assignment lifecycle; preserve comments and documentation.
- Overall status: PARTIAL PASS — code/build and focused tests pass; database migration and authenticated live API remain pending.
- Environment and API base URL: Local source/build; no HTTP base URL exercised.
- Branch, code commit and independently verified deployed build: Current working tree; not committed; deployment not verified.
- Run time and timezone: 2026-09-24, Asia/Calcutta; exact run time not retained.
- Prior report / related UI handoff: [UI handoff](../../../UIdeveloperDoc/EMPLOYEE_WORK_LOCATION_ARRANGEMENT_VALIDATION.md)

## Prerequisites and test data

No live tenant records or credentials were used. Automated tests use deterministic enum/date inputs and inspect the repository, handler, model, and migration contracts. Live API verification requires an authenticated Tenant role with dynamically resolved Work Location and Work Arrangement module-operation permissions.

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| LOC-01 | Build `AxionPro.sln --no-restore` | Compile with 0 errors | 0 errors; existing warnings | local | PASS | Build output summarized below |
| LOC-02 | Focused validation tests | Mode/type, coverage, overlap, typed policy configuration, handler/repository and DDL contracts pass | 33 passed, 0 failed, 0 skipped | local | PASS | Test command below |
| LOC-03 | Apply migration | Replace global unique indexes and install overlap exclusion constraints | Not run | database | PENDING | `database-scripts/AlignEmployeeWorkLocationAndArrangementValidation.sql` |
| LOC-04 | Authenticated create/update/status/delete API flow | Reject inconsistent and overlapping records; allow non-overlapping history/future | Not run | local/live API | PENDING | Requires authorized runtime and migrated DB |
| LOC-05 | Published policy location-scope cross-check | Stable typed generic-policy scope is enforced | One-to-one typed configuration and Work Arrangement validation implemented | local build/unit | PASS | `Attendance_policy_configuration_controls_physical_and_wfh_arrangements_without_device_enrollment` |
| LOC-06 | Local API-host smoke | Anonymous protection, client detection and Swagger respond while API is running | 3 passed, 0 failed, 0 skipped | local HTTP | PASS | focused smoke command below |
| LOC-07 | Full automation project | Record complete regression state | 528 passed, 5 failed, 95 skipped | local | PARTIAL | Three HTTP cases initially ran before host startup; two unrelated pre-existing source-contract failures remain |

The typed table is deployed by `database-scripts/AddAttendancePolicyVersionConfiguration.sql`. The script was source-reviewed and automated contract-tested in this run; it was not executed against a live database. Existing Attendance versions are inserted only when missing, with compatibility settings, and existing typed rows are never overwritten.

## Investigation and changes

- Observed failure: Location and arrangement records were validated independently; active checks ignored effective-window overlap.
- Confirmed cause and evidence: Repository methods filtered only active/non-deleted rows; Work Arrangement accepted any active Tenant location and did not require a covering employee assignment.
- Resolved design gap: Generic PolicyVersion now owns a typed one-to-one `AttendancePolicyVersionConfiguration`; no rule-JSON parsing or legacy AttendancePolicy dependency is used.
- Changed files and commit: See current working tree; no commit created.
- What the executed regression tests establish and do not establish: Build proves compilation. Focused tests establish deterministic rule and source contract behavior, but cannot prove migrated database or live authorization behavior.

## Automated verification

- Test project, exact test names/categories and command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "EmployeeWorkArrangement|EmployeeWorkLocation|PolicyFramework" --logger "console;verbosity=minimal"`
- Counts: 33 passed / 0 failed / 0 skipped / 33 total.
- Local API smoke command after starting `axionpro.api` on port 5170: filter for `Protected_navigation_endpoint_rejects_an_anonymous_request`, `Detect_device_returns_the_automation_client_context`, and `Swagger_document_is_available_and_contains_routes`; 3 passed / 0 failed / 0 skipped.
- Full suite command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --logger "console;verbosity=minimal"`; 528 passed / 5 failed / 95 skipped. Three connection-refused failures were rerun successfully with the API host active. The remaining two failures concern the pre-existing token-only attendance-policy dropdown characterization and unrelated EmployeeType seed expectations; they were not changed or counted as passes.
- Environment prerequisites and skip reasons: No database required for focused tests.
- Sanitized output location or recorded summary: recorded in this report.
- Build result: PASS, 0 errors; existing repository warnings remain.

## Persistence and cleanup

No database rows were created or changed. No migration was executed. No cleanup was required.

## Remaining acceptance and completion criteria

- Apply and verify the migration in the intended database before deployment.
- Run authenticated negative and positive API flows after migration.
