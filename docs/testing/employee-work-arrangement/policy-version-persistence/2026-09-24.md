# Employee Work Arrangement: PolicyVersion persistence — 2026-09-24

## Scope and status

- Scenario ID: EWA-POLICY-VERSION-2026-09-24
- User-requested behavior: the Attendance Policy dropdown selection must save successfully and must not produce the misleading Hybrid-type validation error.
- Overall status: PASS locally; configured target DB migration verified; deployed API/UI acceptance PENDING.
- Environment: local source/build on Windows; no authenticated live mutation executed.
- Related UI handoff: [Attendance Policy options and Work Arrangement integration](../../../UIdeveloperDoc/EMPLOYEE_WORK_ARRANGEMENT_ATTENDANCE_POLICY_OPTIONS.md)

## Prerequisites and test data

Create/Update retains the existing dynamic module and operation permission flow. Numeric permission IDs are not hard-coded. The dropdown remains token-only and supplies an exact Published Attendance `policyVersionId` for the selected `effectiveFrom` date.

Illustrative write fields:

```json
{
  "employeeId": "<encoded-employee-id>",
  "policyVersionId": 9,
  "workMode": 3,
  "hybridType": 2,
  "effectiveFrom": "2026-09-24",
  "isActive": true
}
```

## Steps and results

| Case | Action | Expected | Actual | Environment | Status |
| --- | --- | --- | --- | --- | --- |
| EWA-01 | Build API project | New entity, DTO, mapper, repository and handler contract compiles | 0 errors; existing warnings remain | Local | PASS |
| EWA-02 | Run EmployeeWorkArrangement category | Dropdown authentication and new persistence/schema assertions pass | 9 passed, 0 failed, 0 skipped | Local | PASS |
| EWA-03 | Run form schema tests | Hybrid type required only for Hybrid; policy version remains required | 9 passed, 0 failed | Local | PASS |
| EWA-04 | Build Angular production bundle | Updated request/response interfaces and template compile | Build completed; existing bundle-budget warnings remain | Local | PASS |
| EWA-05 | Apply additive SQL migration | `PolicyVersionId` column/index/FK available | Nullable bigint column, FK, index and nullable legacy column verified | Configured target DB | PASS |
| EWA-06 | Authenticated create/read-back | Selected exact PolicyVersion is persisted and returned | Not executed; requires migrated/deployed target | Deployed | PENDING |

## Investigation and changes

- Confirmed cause: the dropdown returned `policyVersionId`, while the form mapped a nonexistent legacy ID and submitted `0` as `attendancePolicyId`.
- The backend's combined validation condition then returned a Hybrid configuration message even though the failing value was the policy ID.
- Create/Update now accept and validate `PolicyVersionId`. Validation checks Tenant, `ATTENDANCE` category, active/non-deleted type and policy, active `PUBLISHED` version, and the version effective window against the Work Arrangement start date.
- Validation messages are separated so an invalid policy version cannot surface as a Hybrid-type error.
- The Angular form stores and submits the returned `policyVersionId`; it sends `hybridType` only for Hybrid work.
- The migration retains nullable legacy `AttendancePolicyId` for historical rows and performs no guessed backfill.

## Automated verification

- Backend: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "TestCategory=EmployeeWorkArrangement" --nologo --verbosity:minimal`
- Backend count: 9 passed / 0 failed / 0 skipped / 9 total.
- Frontend schema: `bun test .../employee-work-arrangement-form.schema.spec.ts`
- Frontend schema count: 9 passed / 0 failed / 9 total.
- Frontend Angular store tests: 18 passed / 0 failed across 2 files.
- Frontend production build: `bun run build` — PASS with existing size-budget warnings.
- Direct `bun test` for Angular stores is not a valid isolated runner in this repository because Angular's JIT setup is supplied by the Angular test builder; that attempted invocation produced the expected missing-compiler harness error and is not recorded as a product failure.

## Persistence and cleanup

The source migration is `database-scripts/AddPolicyVersionToEmployeeWorkArrangement.sql`. It was applied transactionally to the configured target. Verification confirmed nullable bigint `PolicyVersionId`, its index and FK, and nullable legacy `AttendancePolicyId`. The migration contains no data update/backfill, so existing row values were not rewritten.

## Remaining acceptance and completion criteria

- Deploy matching API and UI builds.
- Execute an authenticated Hybrid create plus read-back, and a non-Hybrid create with `hybridType: null`.
- Verify rejection of Draft, expired, future, wrong-category and other-tenant PolicyVersion IDs against an isolated test dataset.
