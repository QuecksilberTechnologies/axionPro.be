# Employee: China and USA identity options — 2026-09-13

## Scope and status

Create one China employee and one USA employee in the authorized development
tenant, then inspect the identity/document options offered to each employee.

**Overall: PENDING.** China creation succeeded; the identity read failed before
the fix. USA creation is blocked by missing live country options. No successful
post-fix deployed identity response is recorded.

- Live target: `https://axionpro-api.onrender.com` (development).
- Actor: authorized Tenant Admin; credentials and tokens excluded.
- Local fix: `05390640` on `main_branch`, committed and pushed.
- Exact live build during the failed identity request: not captured.
- Test date: 2026-09-13; exact run timestamp not retained.
- Original evidence: [Render smoke report](../../../RENDER_SMOKE_TEST_2026-09-13.md).
- This report consolidates the preceding run. No tests were repeated to write it.

## Prerequisites and input

Use authenticated menu/permission discovery for current ModuleId/OperationId.
Country and document identifiers are master data; select their actual returned
IDs. The recorded China CountryId `2` and EmployeeId `78N5XZW2` apply only to this
test environment and are not UI constants.

Recorded disposable China employee:

| Field | Recorded value |
| --- | --- |
| EmployeeId, returned by API | `78N5XZW2` |
| Email | `china.identity.20260913070255@example.test` |
| CountryId | `2` |
| CountryCode / nationality read-back | `CN` / `China` |

The complete original employee-create JSON was not retained in the existing
report. These fields are a partial record of test data, not a complete replayable
create request. No USA employee identifier exists for this run.

Identity query example (placeholders must be replaced; this is not a raw log):

```http
GET /api/Employee/Sensitive/get?EmployeeId=78N5XZW2&CountryNationalityId=2&ModuleId=<granted-identity-module-id>&OperationId=<granted-view-operation-id>
Authorization: Bearer <access-token>
```

`EmployeeId` selects the encoded target employee; omitting it uses the caller's
employee context. `CountryNationalityId` is a legacy integer hint in the request
DTO. The handler reads the target employee's persisted `CountryId` to decide
eligible documents; changing the query hint must not select another country's
documents. Authentication, permission and target-employee access checks precede
the lookup. This describes reviewed code, not an executed isolation test.

## Executed cases

| Case | Action | Expected | Actual | Status |
| --- | --- | --- | --- | --- |
| COUNTRY-01 | Live `GET /api/Location/country/option`, existing actor/date query | Discover valid country IDs | Only India (1) and China (2) returned; USA absent | PASS for discovery; USA prerequisite missing |
| CN-01 | Live `POST /api/Employee/create` | Create China employee | Employee `78N5XZW2` returned | PASS |
| CN-02 | Live employee-list read-back | Persisted country remains China | CountryId 2, CountryCode CN, Nationality China | PASS via API; no direct DB reconciliation |
| CN-03 | Live `GET /api/Employee/Sensitive/get` for China employee | Successful identity option response | HTTP 500 | FAIL before fix |
| US-01 | Create USA employee | Valid USA CountryId available | Active country option missing; creation not attempted | BLOCKED |
| US-02 | Read USA identity/document options | Successful country-specific option response | No USA employee created | BLOCKED |
| CN-04 | Repeat identity GET after deploying `05390640` | Successful option response using deployed fix | Not executed/verified in recorded evidence | PENDING |

The original raw success/error bodies were not retained. HTTP status and
read-back facts above are recorded session evidence, not new API calls.
No identity number or identity document file was uploaded in this scenario.

## Expected catalogue options — seed review only

Source: [SeedEmployeeCountryIdentityCatalog.sql](../../../../database-scripts/SeedEmployeeCountryIdentityCatalog.sql).

| Country | Document code | Document name | New rule default |
| --- | --- | --- | --- |
| China (`CN`/`CHN`) | `NATIONAL_ID_CN` | Resident Identity Card | `IsMandatory=false` |
| United States (`US`/`USA`) | `SSN` | Social Security Number | `IsMandatory=false` |

These are **seed expectations, not options returned successfully by the live
API**. The seed creates missing document catalogue entries and country mappings
only where a matching active Country exists. It does not insert Country rows.
It preserves existing configured rules, including mandatory flags. Other
configured active mappings can also affect the actual returned options.

On a successful lookup the response data is an array containing
`identityCategoryDocumentId`, `documentCode`, `documentName`, `countryCode`,
`countryName`, `isMandatory` and optional saved identity information. An option
without a saved EmployeeIdentity can have null employee/identity IDs and values.
Do not treat an available option as proof that a document was uploaded.

## Failure investigation and implemented fix

- Confirmed live symptom: China identity GET returned HTTP 500.
- Reviewed old code called PostgreSQL `GetEmployeeIdentityByCountryRule`.
  Its definition was found in `axionpro.application/DTOS/SPFunctions.txt`, not
  in an executable migration/seed found during that review. A missing or
  mismatched deployed function is a **suspected cause**; a server exception or
  direct target-function inspection was not captured to prove it caused this 500.
- Commit `05390640` removes that function dependency from
  `StoreProcedureRepository.GetIdentityRecordAsync`, using an EF country-rule
  query. Existing handler authorization remains in place.
- A confirmed projection defect copied `EffectiveFrom` into `EffectiveTo`.
  The fix uses the correct end date and maps the internal date fields as
  `DateOnly?`, retaining the response's nullable DateTime fields.
- Passing local tests establishes the recorded local assertions only. It does
  not establish deployed query execution, country catalogue seeding, or delivery
  of the expected options.

## Automated verification already executed

```powershell
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~EmployeeProfileCharacterizationTests|FullyQualifiedName~EmployeeCountryRuleDatabaseTests" --no-restore --verbosity minimal
```

Result: **18 passed, 0 failed, 5 skipped; total 23**. The final test build
succeeded. Raw output is in the preceding session; no dedicated raw log was
retained with this report.

New regression:
`EmployeeProfileCharacterizationTests.Identity_projection_keeps_distinct_effective_from_and_to_dates`
checks that different start/end dates remain different in the API DTO and that
the employee ID is projected through the encoder.

Five database tests were skipped because `AXIONPRO_BULK_TEST_CONNECTION` was not
configured for the disposable `axionpro_bulk_test` database:

1. `Identity_catalogue_rejects_foreign_tenant_and_inactive_rule`
2. `Operational_defaults_are_locked_until_enabled_and_can_be_locked_again`
3. `Tenant_role_loaded_for_update_is_detached_and_persists_changed_name`
4. `Tenant_rule_beats_global_rule_and_expired_rule_is_ignored`
5. `Verified_bank_cannot_be_reopened_and_foreign_tenant_bulk_update_is_rejected`

The database identity eligibility test targets `EmployeeIdentityRepository`;
it does not execute the changed `StoreProcedureRepository.GetIdentityRecordAsync`
query. The recorded 18 passes therefore do not establish a database regression
pass for the replacement query or China/USA catalogue coverage.

## Persistence, retention and cleanup

- China employee retained as requested; country was confirmed through API
  read-back. Its underlying Employee/account/contact rows were not directly
  reconciled against the DB in this recorded scenario.
- USA employee was not created.
- Identity option reads use Country, CountryIdentityRule, IdentityCategory,
  IdentityCategoryDocument and existing EmployeeIdentity data; the GET does not
  create uploaded identity records.
- No document-file persistence or retention policy was exercised. No cleanup
  or automatic expiry was performed or verified for the China test employee.

## Remaining acceptance

1. Verify the deployed build contains `05390640`; capture the successful China
   identity response and actual active document mappings.
2. Resolve the missing USA Country prerequisite through the authorized master
   data flow and ensure its intended active identity mapping exists.
3. Create one USA test employee, read its country back, and capture the actual
   identity/document option response.
4. Record any required database-query regression and DB reconciliation results
   separately. Confirmed deployment/live evidence is required before marking
   this scenario complete.
