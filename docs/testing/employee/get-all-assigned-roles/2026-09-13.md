# Employee: get-all assigned roles — 2026-09-13

## Scope and status

- Scenario ID: EMP-GETALL-ASSIGNED-ROLES
- User-requested behavior / acceptance criteria: add `assignedRoles` to every
  existing Employee-list row, with raw Role id, code/name/type, no duplicates and
  an empty array when no role is effective.
- Overall status: PASS locally for build and serialization contract; database
  fixture execution and deployed acceptance remain PENDING.
- Environment and API base URL: local source/build; Render API not called in this run.
- Branch, code commit and independently verified deployed build: `main_branch`;
  commit and deployed build pending.
- Run time and timezone: 2026-09-13 Asia/Calcutta; exact time not retained.
- UI handoff: [Employee list assigned roles](../../../UIdeveloperDoc/EMPLOYEE_GET_ALL_ASSIGNED_ROLES.md)

## Prerequisites and test data

The authenticated request uses the existing Employee-list View permission. The UI
must resolve ModuleId/OperationId dynamically. Automated database verification is
guarded to the disposable database named `axionpro_bulk_test`; that connection was
not configured in this run.

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| EMP-ROLE-01 | Build automation project in Release | Compiles with no errors | Build succeeded, 0 errors | local | PASS | Build command below |
| EMP-ROLE-02 | Serialize role-bearing and role-free Employee DTOs | `assignedRoles` contains approved fields or `[]` | 1 focused test passed | local | PASS | `Employee_list_role_contract_serializes_assigned_roles_and_empty_array` |
| EMP-ROLE-03 | Query duplicate/current/cross-tenant role assignments | Only distinct active same-Tenant roles returned | Test implemented; isolated DB connection unavailable | local isolated DB | NOT RUN | `Employee_list_returns_distinct_active_tenant_roles_with_stable_contract` |
| EMP-ROLE-04 | Call deployed `GET /api/Employee/get-all` | Live rows contain accurate `assignedRoles` | Deployment/live request not executed | Render | PENDING | Next deployment required |

## Investigation and changes

- Confirmed cause: the existing Employee-list DTO and repository projection did
  not load `UserRole`/`Role` data.
- Change: one page-batched role query now applies tenant, active and soft-delete
  filters, collapses duplicate Role IDs and populates `assignedRoles`.
- Schema fact: `Role` has no separate RoleCode column. `code` is therefore derived
  from `RoleName` using uppercase underscore formatting; `type` derives from
  `IsSystemDefault`.
- The executed contract test establishes JSON property names and empty-array
  behavior. It does not establish deployed data accuracy.

## Automated verification

```powershell
dotnet build axionpro.automationtests/axionpro.automationtests.csproj -c Release --no-restore --nologo -v:minimal
dotnet test axionpro.automationtests/axionpro.automationtests.csproj -c Release --no-build --filter "FullyQualifiedName~Employee_list" --logger "console;verbosity=normal"
```

- Build: succeeded, 0 errors; existing package-advisory warnings remain.
- Focused run: 1 passed / 0 failed / 1 skipped / 2 total.
- The skipped database test is guarded by `AXIONPRO_BULK_TEST_CONNECTION` and is
  not counted as a pass.

## Persistence and cleanup

This GET endpoint performs no writes. It reads the Employee page and effective
assignments from `UserRole` joined to `Role`. No cleanup is required.

## Remaining acceptance and completion criteria

- Run the database test against local isolated `axionpro_bulk_test`.
- Deploy the committed build and capture a sanitized live response showing an
  Employee with roles and, if available, an Employee with `assignedRoles: []`.
