# Employee Reset Password module consolidation — 2026-09-23

## Result

**PASS on the Development-configured database and local automated suite.** The obsolete standalone Module Id 38 was removed. Existing Reset Password entitlements and the role grant were moved to `EMP_LIST`, while Operation Id 21 remained active.

## Environment and scope

- Date: 2026-09-23
- Repository: local working tree
- Database: connection configured by `axionpro.api/appsettings.Development.json`
- Scope: module/operation seed, database migration, Employee permission resolution and documentation
- Deployment verification: not run
- Authenticated HTTP reset request: not run because this change concerns permission ownership and seed/database records; no user password was changed for this test

## Preconditions observed

- Module Id 38: `EMP_PASSWORD_MANAGEMENT`
- Employee list module: Id 8, code `EMP_LIST`
- Operation Id 21: `Reset Password`
- Module 38 had one module-operation mapping, one plan mapping, one tenant module entitlement, one tenant operation entitlement and one role permission.
- A sanitized pre-change record backup was written to `DBFullBACKUP/Module38-before-removal-20260923.txt`.

## Executed change

`database-scripts/MoveResetPasswordToEmployeeList.sql` was executed against the configured database. The migration:

1. verifies `EMP_LIST` and Operation Id 21;
2. refuses cleanup if a legacy tenant or plan lacks its required `EMP_LIST` entitlement;
3. creates/updates the `EMP_LIST` + Reset Password mapping;
4. copies the legacy tenant operation entitlement and role grant to `EMP_LIST`;
5. removes Module Id 38 rows from permission, tenant, plan and operation mappings;
6. removes Module Id 38 itself; and
7. verifies the final state before commit.

The migration was rerun once to verify idempotency; the second execution completed successfully without recreating Module 38 or duplicating the mapping.

## Database verification

| Assertion | Actual |
| --- | ---: |
| Module Id 38 / `EMP_PASSWORD_MANAGEMENT` rows | 0 |
| Active `EMP_LIST` + Operation Id 21 mappings | 1 |
| Module Id 38 `ModuleOperationMapping` rows | 0 |
| Module Id 38 `TenantEnabledModule` rows | 0 |
| Module Id 38 `TenantEnabledOperation` rows | 0 |
| Module Id 38 `RoleModuleAndPermission` rows | 0 |
| Module Id 38 `PlanModuleMapping` rows | 0 |
| Active Operation Id 21 (`Reset Password`) | 1 |
| `EMP_LIST` tenant operation entitlement rows for Operation 21 | 1 |
| `EMP_LIST` active role grant rows for Operation 21 | 1 |

## Automated validation

Command:

```powershell
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~ResetPasswordModuleConsolidation" --no-restore --verbosity minimal -p:NuGetAudit=false
```

Connection environment variable `AXIONPRO_MODULE_TEST_CONNECTION` was populated from the local Development settings only for the test process and was then removed.

Result: **3 passed, 0 failed, 0 skipped**.

Covered tests:

- Reset Password commands resolve the required module code as `EMP_LIST`.
- The migration and consolidated seed contain the Module 38 cleanup and `EMP_LIST` mapping contract.
- The configured database has no Module 38 dependency and retains Operation Id 21, tenant entitlement and role permission under `EMP_LIST`.

The build emitted existing compiler and NuGet advisory warnings. No build or test error occurred.

## Persistence and cleanup

The database change is intentional and retained. No employee credential or password was used or modified. The pre-change module metadata backup remains in `DBFullBACKUP` for recovery evidence.

## Pending

- Deployed environment verification is pending.
- An authenticated UI/API permission acceptance check is pending; the UI must resolve module and operation IDs through the existing authenticated menu/permission response.

