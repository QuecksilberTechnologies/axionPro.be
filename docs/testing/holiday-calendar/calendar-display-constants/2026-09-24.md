# Holiday calendar: display constants — 2026-09-24

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-COLORS-001`
- Acceptance criteria: authenticated read-only API returns stable mandatory/optional holiday, Taken, Pending and working-day colors with deterministic overlap priority; no permission IDs or database table required.
- Overall status: PASS locally for build-through-test, focused contract suite and unauthenticated HTTP; authenticated and deployed calls remain PENDING.
- Environment: local Development API, `http://127.0.0.1:7788`.
- Working tree only; no deployment asserted.
- UI handoff: [Holiday](../../../UIdeveloperDoc/HOLIDAY.md).

## Steps and results

| Case | Action | Expected | Actual | Status |
| --- | --- | --- | --- | --- |
| HCC-01 | Compile and run `HolidayCrud` test category | Endpoint/constants compile; contracts pass | 4 passed, 0 failed, 0 skipped | PASS |
| HCC-02 | Inspect request contract | No ModuleId/OperationId | Query has no permission fields; permission behavior does not intercept it | PASS |
| HCC-03 | Call without token | HTTP 401 standard envelope | HTTP 401, `UNAUTHORIZED` | PASS |
| HCC-04 | Call with valid Tenant token | HTTP 200 five styles | Not run; no test token supplied | PENDING |
| HCC-05 | Call deployed API | Same contract | Not run | PENDING |

Observed unauthenticated response:

```json
{"IsSucceeded":false,"Message":"The request is not authenticated.","Data":null,"Errors":[],"ErrorCode":"UNAUTHORIZED"}
```

## Automated verification

Command:

```powershell
dotnet test axionpro.automationtests\axionpro.automationtests.csproj --no-restore --filter "Category=HolidayCrud"
```

Result: 4 passed / 0 failed / 0 skipped / 4 total. The build emitted existing project/package warnings and no new compile error.

## Persistence and cleanup

The endpoint returns constants from application code. It reads/writes no Holiday or leave rows and creates no files. The local API process was stopped after the 401 smoke check.

## Remaining acceptance

- Run authenticated local response check and verify all five style records.
- Verify the deployed build after release.
- Employee leave-history/request aggregation remains a separate API concern; this endpoint supplies display metadata only.
