# Holiday calendar: tenant-scoped CRUD — 2026-09-22

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-CRUD-001`
- User request: complete CRUD in the existing HolidayCalandar controller, refactor GET, and use the established tenant permission flow.
- Overall status: local build, focused contract tests and rollback-only PostgreSQL repository test PASS; authenticated end-to-end and deployed CRUD PENDING.
- Environment: Windows workspace, .NET 10. Development-configured PostgreSQL schema was previously migrated; no authenticated API base URL was exercised here.
- Branch/commit: current working tree; commit not created.
- Run date/timezone: 2026-09-22, Asia/Calcutta; exact time not captured.
- UI handoff: [Organization Holiday Calendar](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md).

## Prerequisites and test data

The authenticated tenant must have an active `TenantLocation` and permissions for `TENANT_POLICY_HOLIDAY_CALENDAR`. Module and operation IDs must be resolved from the authenticated menu/permission flow. The repository test used an existing active location and a temporary holiday inside a rolled-back transaction. The handoff includes illustrative sanitized JSON; it is not a captured HTTP request or response.

## Steps and results

| Case | Action / method / endpoint | Expected | Actual | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| HC-CRUD-01 | Build API project | Compiles with CRUD handlers/controller | 0 errors | Local | PASS | `dotnet build` command below |
| HC-CRUD-02 | Focused route/DTO/handler tests | Five controller routes, permission DTOs and handlers exist | 3 CRUD contract tests passed | Local | PASS | `dotnet test` command below |
| HC-CRUD-03 | Existing holiday refactor tests | Entity/DTO/migration contract remains valid | 3 tests passed | Local | PASS | `dotnet test` command below |
| HC-CRUD-04 | Repository create/read/update/soft-delete in rolled-back transaction | Persistence, duplicate detection and tenant filtering | Passed; post-test DB count for test name was 0 | Target DB | PASS | `HolidayCalendarCrudDatabaseTests` |
| HC-CRUD-05 | Authenticated POST/PUT/DELETE | Tenant-owned location, duplicate validation, soft delete and read-back | Not called | API | PENDING | No token/API call |
| HC-CRUD-06 | Authenticated GET list/detail | Tenant-only rows; location/year filters and 404 | Not called | API | PENDING | No token/API call |
| HC-CRUD-07 | Denied operation / foreign tenant | 403/404 and no mutation | Not called with a real account | API | PENDING | No test-account credentials available |
| HC-CRUD-08 | Start local API and call five CRUD routes without token | Every route rejects unauthenticated request | GET list, GET detail, POST, PUT and DELETE each returned HTTP 401 | Local HTTP | PASS | `Invoke-WebRequest` smoke in task session |
| HC-CRUD-09 | Permission pipeline with module/operation/grant combinations | Only correct module + Add + allowed grant invokes create handler | 4 parameterized cases passed; wrong module, View operation and denied grant stopped | Local unit | PASS | `HolidayCalendarPermissionTests` |

## Investigation and changes

The old GET handler called an unscoped `GetAllHolidaysAsync` and had no runtime permission check. The controller now routes through tenant-scoped handlers for list, detail, create, update and delete. A dedicated MediatR permission behavior uses the existing authenticated tenant validation, module-code lookup, operation lookup, stored-procedure permission check and `TenantRuntimePermissionValidator`. The old unscoped GET handler was removed. The repository validates active tenant-owned locations, reads only tenant-owned active holidays, rejects duplicate name/date/location combinations and soft-deletes with audit fields. No holiday policy generation or bulk import was added.

## Automated verification

- Build command: `dotnet build axionpro.api/axionpro.api.csproj --no-restore -v quiet -p:NuGetAudit=false` — PASS, 0 errors; existing solution warnings remain.
- Test command: `dotnet test axionpro.automationtests --filter 'HolidayCalendarCrud|OrganizationHolidayCalendarRefactor' --no-restore --verbosity quiet -p:NuGetAudit=false`
- Result: 6 passed / 0 failed / 0 skipped / 6 total for route/schema tests. CRUD contract tests verify routes and request contracts, not authorization decisions.
- CRUD test names: `Controller_requires_authentication_and_exposes_all_crud_routes`, `Request_contract_requires_dynamic_permission_ids_and_location_scope`, `Every_crud_request_has_a_permission_pipeline_handler`. Existing refactor test names: `Entity_uses_location_and_date_without_redundant_geography_fields`, `Response_contract_exposes_location_and_nonredundant_holiday_fields`, `Migration_is_guarded_and_creates_fk_and_location_date_index`.
- PostgreSQL behavior test command: set `AXIONPRO_HOLIDAY_TEST_CONNECTION` to the Development connection string, then `dotnet test axionpro.automationtests --filter HolidayCalendarCrudDatabase --no-restore --verbosity quiet -p:NuGetAudit=false`. Result: 1 passed / 0 failed / 0 skipped / 1 total. Test name: `Repository_create_read_update_and_soft_delete_stays_tenant_scoped`. The connection string was not written to the report or committed.
- Permission test command: `dotnet test axionpro.automationtests --filter HolidayCalendarPermission --no-restore --verbosity quiet -p:NuGetAudit=false`. Result: 4 passed / 0 failed / 0 skipped / 4 parameterized cases. Test name: `Create_reaches_handler_only_with_matching_module_operation_and_grant`.
- Local HTTP smoke: `dotnet run --no-build --project axionpro.api/axionpro.api.csproj --launch-profile http`, then unauthenticated requests to all five CRUD routes on `http://localhost:5170`; each returned HTTP 401. The locally started API was stopped afterward.
- No deployed build or API response was verified. No raw API response was captured.

## Persistence and cleanup

One temporary holiday was created, updated and soft-deleted inside a PostgreSQL transaction, then rolled back. A direct post-test count by its test name returned 0. Existing DB migration and module-operation mappings remain. No permanent holiday record or file was created.

## Remaining acceptance and completion criteria

1. With an authorized tenant test account, call every route and reconcile `OrganizationHolidayCalendar` rows and audit fields. No dedicated tenant login credentials were available in this task's environment, so this was not marked PASS.
2. Test missing/foreign-tenant location, duplicate holiday, wrong operation, denied permission, cross-tenant ID and repeat delete.
3. Verify the deployed build and UI behavior separately; local compilation does not establish deployed acceptance.
