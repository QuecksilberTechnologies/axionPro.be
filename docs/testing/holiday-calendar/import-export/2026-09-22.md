# Holiday calendar: import and export — 2026-09-22

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-TRANSFER-001`.
- Requested behavior: CSV/XLSX holiday import, CSV export, Import/Export module-operation mapping and seed, API and UI handoff.
- Status: local build, automated tests and target-DB seed rerun PASS. Authenticated HTTP, deployed verification and isolated seed fixture PENDING; release acceptance remains WIP.
- Environment: Windows workspace, .NET 10, Development-configured PostgreSQL `workforcedb_34hi_duis`. Branch working tree; no commit or deployed build verified. Run date 2026-09-22 Asia/Calcutta; exact time not captured.
- Handoff: [Organization Holiday Calendar](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md). Related [CRUD report](../crud/2026-09-22.md).

## Prerequisites and test data

The authenticated tenant employee needs `TENANT_POLICY_HOLIDAY_CALENDAR` Import or Export permission and at least one active tenant-owned `TenantLocation`. The UI resolves ModuleId and OperationId from the existing authenticated menu/permission flow; numeric examples in the handoff are illustrative. Import accepts one CSV/XLSX file or pasted table, with `TenantLocationId,HolidayName,HolidayDate,IsOptional,Description`; dates are `YYYY-MM-DD`. The PostgreSQL test used a temporary `Calendar Import Verification` holiday inside a rolled-back transaction. No credentials or real employee identity data are retained.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| HC-IE-01 | Build API | Import/export compiles | 0 errors | Local | PASS | Build command below |
| HC-IE-02 | Contract and permission tests | Import/Export routes and operation checks | Included in 14 focused tests; Import permitted, View rejected for import | Local | PASS | Test command below |
| HC-IE-03 | CSV import, export, repeat import, invalid date | Insert one, export it, skip existing, reject invalid input | Rollback-only PostgreSQL test passed | Target DB test transaction | PASS | `HolidayCalendarTransferDatabaseTests` |
| HC-IE-04 | Direct post-test reconciliation | Test data absent after rollback | `import_test_rows = 0` | Target DB | PASS | Direct count by test name |
| HC-IE-05 | Seed and rerun | Six distinct CRUD + Import/Export mappings | Import priority 50 and Export priority 60; one of each of six operations | Target DB | PASS | Direct Module/Operation/Mapping query |
| HC-IE-06 | Authenticated import/export HTTP | Correct status, payload, file, tenant isolation | Not called | API | PENDING | No authenticated test account used |
| HC-IE-07 | Isolated PostgreSQL seed first-run/rerun | Full seed succeeds in disposable fixture | Not run | Isolated fixture | PENDING | Fixture unavailable |

No raw authenticated API response was captured. The UI handoff gives illustrative requests/responses and explicitly distinguishes them from observed traffic.

## Investigation and changes

The existing holiday controller had CRUD only. Import and Export now use the same tenant permission behavior with their respective operation mappings. Import validates every row and active tenant location before saving, rejects duplicates within the file, skips matching active database records, and returns created/skipped counts. Export emits tenant-scoped UTF-8 CSV with the same column headers. The import is synchronous and bounded by the shared table reader (5 MB, 5,000 rows); it is not a durable BulkImportMaster job. Both production and tenant-policy seed files include Import/Export mappings. No observed functional failure remains from the focused tests. Authenticated network behavior and full-seed portability remain unverified.

## Automated verification

- Build: `dotnet build axionpro.api/axionpro.api.csproj --no-restore -v quiet -p:NuGetAudit=false` — PASS, 0 errors; existing warnings remain.
- Tests: set `AXIONPRO_HOLIDAY_TEST_CONNECTION` from the Development configuration for this local run, then `dotnet test axionpro.automationtests --filter 'HolidayCalendar|OrganizationHolidayCalendarRefactor' --no-restore --verbosity quiet -p:NuGetAudit=false` — 14 passed, 0 failed, 0 skipped, 14 total. The environment variable was removed afterward; its value was not recorded.
- Tests include schema/refactor, CRUD contract, permission behavior, rollback-only CRUD repository and rollback-only CSV import/export database cases. These establish local handler/repository behavior, not deployed HTTP acceptance.
- Seed: `SeedTenantPolicyModules.sql` was run twice against the configured target DB and queried afterward. The matching module has exactly one each of View, Add, Update, Delete, Import and Export mappings, at priorities 10/20/30/40/50/60. This is target-DB evidence, not isolated full-seed acceptance.

## Persistence and cleanup

The import test inserted a temporary row in `axionpro."OrganizationHolidayCalendar"`, exported it, repeated the import, validated a bad date, and rolled back. A direct post-test query returned zero rows with its test name. No import file is retained by the API. Seed mappings in `axionpro."ModuleOperationMapping"` remain in the target DB. No deployed DB was verified.

## Remaining acceptance and completion criteria

1. Run authenticated API import/export with the current Import/Export permission IDs; capture sanitized status/body/file and reconcile tenant-owned rows, foreign-location rejection and denied-operation behavior.
2. Run the complete seed in an isolated PostgreSQL fixture on first run and rerun, checking duplicate-free module, operation, mapping and plan links.
3. Verify the intended deployed build separately before calling this production accepted.
