# Holiday calendar: 2026 Jabalpur live-data smoke — 2026-09-22

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-LIVE-2026-JBP`.
- User-confirmed scope: year 2026, tenant 8, `IN-MP-JBP Head Office` (active `TenantLocationId=5`); populate January through December with full holiday fields and smoke test.
- Result: target Development-configured DB insertion/read-back PASS; local unauthenticated API smoke PASS; 14 focused tests PASS. Authenticated HTTP import/export and company holiday approval NOT VERIFIED.
- Environment: local API `http://localhost:5170`, PostgreSQL `workforcedb_34hi_duis`; current working tree, no deployed build verified. Date 2026-09-22 Asia/Calcutta, exact time not captured.
- Source: [Madhya Pradesh Commercial Tax Appellate Board 2026 government and optional holiday list](https://mpctab.mp.gov.in/holidays), which cites General Administration Department notification F-3-1-2025-1-4 dated 29 December 2025. Government dates are a reference for this tenant calendar, not proof that the private employer approved every closure.
- Related [API/UI handoff](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md) and [import/export report](../import-export/2026-09-22.md).

## Prerequisites and test data

The target DB had no holiday rows and showed one active tenant-8 location, Id 5. The input [CSV](holidays-2026.csv) contains 31 real named holidays with date, optional flag and source/meaning in Description: all 29 holidays in the source's government list, plus two source-listed optional holidays (New Year's Day in January and Guru Purnima in July) so each month has a row. This does **not** import all 63 optional holidays from the source. Sunday/Saturday holidays remain on their actual dates, with weekday noted in Description where relevant. `IsOptional=false` reflects the source's government-list category; it is not a company approval decision. Numeric tenant/location IDs are observed in this DB only.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| HC-LIVE-01 | Inspect target location and existing holiday rows | Active tenant 8 location; avoid conflicting rows | Location 5 active; holiday table empty before insertion | Target DB | PASS | Direct SQL read-back |
| HC-LIVE-02 | Run [transactional insertion](insert-2026.sql) | 31 rows, all 12 months, valid tenant/location | `COPY 31`, `INSERT 0 31`, COMMIT | Target DB | PASS | `psql -X -v ON_ERROR_STOP=1 -f .../insert-2026.sql` |
| HC-LIVE-03 | Rerun same script | Idempotent; no duplicates | `INSERT 0 0`, COMMIT | Target DB | PASS | Second script run |
| HC-LIVE-04 | Verify month/field coverage | 12 months; optional and descriptions correct | 31 rows; 12 months; 2 optional; 0 missing descriptions/timestamps; 0 duplicate active keys | Target DB | PASS | Direct aggregate queries |
| HC-LIVE-05 | Local GET list and export without token | Authentication rejects requests | Both HTTP 401 | Local HTTP | PASS | `Invoke-WebRequest` smoke |
| HC-LIVE-06 | Local multipart import without token | Authentication rejects valid multipart route | HTTP 401 | Local HTTP | PASS | `Invoke-WebRequest` multipart smoke |
| HC-LIVE-07 | Focused regression tests after insertion | Existing API behavior preserved | 14 passed, 0 failed, 0 skipped | Local + rollback DB test | PASS | Command below |
| HC-LIVE-08 | Authenticated API GET/import/export using tenant-8 account | Tenant-scoped read/write, file response | Not run; no authenticated test session | API | PENDING | No captured response |

The first POST without a multipart Content-Type returned 415 due to the route's `[Consumes("multipart/form-data")]` requirement; the corrected multipart request returned 401, as expected without authentication. No authenticated API response or token was captured.

## Investigation and changes

No API code changed. The calendar was populated directly in the configured PostgreSQL DB using a guarded, repeatable SQL script because an authenticated tenant-8 API session was unavailable. Direct SQL does not validate the API permission pipeline or import HTTP behavior. The insertion verifies active tenant/location and source count/month scope before writing, uses a transaction, and skips matching active records on rerun. The CSV is the exact source data used; no generic test-employee names were inserted.

## Automated verification

- `dotnet test axionpro.automationtests --filter 'HolidayCalendar|OrganizationHolidayCalendarRefactor' --no-restore --verbosity quiet -p:NuGetAudit=false` with `AXIONPRO_HOLIDAY_TEST_CONNECTION` set from local Development configuration: 14 passed / 0 failed / 0 skipped / 14 total. The connection string was not recorded and the environment variable was removed afterward.
- Local API launched with `dotnet run --no-build --project axionpro.api/axionpro.api.csproj --launch-profile http`, then stopped after unauthenticated smoke. API GET list/export and correctly formatted multipart import returned 401.
- The earlier API build remains the local build evidence; this run made no source-code changes.

## Persistence and cleanup

The target DB now intentionally retains 31 active rows in `axionpro."OrganizationHolidayCalendar"`, all `TenantId=8`, `TenantLocationId=5`, dates in 2026. Month counts: Jan 2, Feb 2, Mar 6, Apr 3, May 2, Jun 2, Jul 1, Aug 3, Sep 2, Oct 3, Nov 4, Dec 1. Two are optional; all 31 have Description and AddedDateTime. No duplicate active location/date/name keys. No cleanup was requested. No deployed DB was checked.
`AddedById` is null for these SQL-loaded rows because no authenticated API actor
performed the insertion; it was not falsely attributed to an employee.

## Remaining acceptance and completion criteria

1. Tenant policy owner should review the 29 government-listed and 2 optional entries against the organization's approved closure list; update `IsOptional` or remove entries where the company differs. Weekend entries do not imply a compensatory day.
2. With a tenant-8 account, perform authenticated GET and export, compare all 31 rows, then a controlled import/reimport and denied-permission test. Record sanitized HTTP responses and DB reconciliation.
3. Verify the deployed environment separately. This report establishes the configured target DB state, not production deployment.
