# Holiday calendar: Tenant Policies child module — 2026-09-22

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-MODULE-001`
- Request: seed Organization Holiday Calendar as a child of `TENANT_POLICIES` in `axionpro."Module"` and the module seed files.
- Overall status: target DB CRUD permission seed and idempotence PASS; authenticated menu visibility and CRUD API behavior PENDING.
- Environment: Development-configured PostgreSQL `workforcedb_34hi_duis`; Windows workspace. No deployed API call was made.
- Branch/commit: current working tree; no commit created in this task.
- Run date/timezone: 2026-09-22, Asia/Calcutta; exact time not retained.
- UI handoff: [Organization Holiday Calendar](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md).

## Prerequisites and test data

The existing parent was `TENANT_POLICIES` (Id 108). No existing module with a holiday calendar code/name was found. The existing Angular route `/app/holidays` was read for the menu target; the Angular project was not changed. Existing View/Add/Update/Delete operations were reused. No holiday or employee rows were created.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| HM-01 | Inspect parent and duplicate candidates | Parent exists; no matching child | Parent 108; no holiday child | Target DB | PASS | Direct PostgreSQL read-back |
| HM-02 | Run `SeedTenantPolicyModules.sql` | Child, View mapping, plan entitlement | Id 118 created under 108; one View mapping; 10 plan entitlements | Target DB | PASS | Direct PostgreSQL read-back |
| HM-03 | Rerun seed | No duplicate rows | 1 child, 1 View mapping, 10 plan mappings | Target DB | PASS | Direct PostgreSQL read-back |
| HM-04 | Verify authenticated menu for tenant role | Child displayed as authorized | Not run | Deployed API/UI | PENDING | No authenticated call |
| HM-05 | Inspect CRUD mappings in DB and rerun revised seed | View/Add/Update/Delete, one each, stable priorities | View 10, Add 20, Update 30, Delete 40; one mapping each; 10 plan mappings | Target DB | PASS | Direct PostgreSQL read-back |
| HM-06 | Exercise holiday CRUD endpoints | Each action persists/updates/deletes holidays | Not run: endpoints are not exposed by current controller | API | PENDING | Controller source inspection |

## Investigation and changes

Updated both `database-scripts/complete seed data/SeedTenantPolicyModules.sql` and `AxionPro_New_Production_Module_Operation_Seed.sql` with `TENANT_POLICY_HOLIDAY_CALENDAR`, `Organization Holiday Calendar`, `/app/holidays`, page key `tenant-policy-holiday-calendar`, priority 680, and View/Add/Update/Delete operation mappings. The existing seed pipeline created plan entitlements. Add/Update/Delete permissions existed manually at priority 0 when checked after the initial seed; the revised seed set stable priorities 20/30/40. No API endpoint or Angular code was changed. These permission rows do not implement CRUD API behavior.

## Automated verification

No automated test was added or run. The executed PostgreSQL seed and read-back/idempotence checks above establish the database result; they do not establish tenant-specific menu visibility. No build was run because the changes were SQL seed rows only.

## Persistence and cleanup

Module Id 118, four module-operation mappings and 10 plan-module mappings remain in the target DB. They were intentionally retained; no cleanup was performed.

## Remaining acceptance and completion criteria

Verify the new child through the authenticated menu/permission flow for an entitled tenant user and confirm the deployed UI route is available. Holiday write endpoints must be implemented and tested separately before CRUD permissions represent working actions. Numeric IDs here are observed in this database only; consumers must resolve IDs dynamically.
