# Holiday rename and Icon field — 2026-09-23

## Scope

- Rename the domain, API, DTO, feature, repository, unit-of-work and automated-test identity from `OrganizationHolidayCalendar` / `HolidayCalandar` to `Holiday`.
- Rename the PostgreSQL table to `axionpro."Holiday"` without losing rows.
- Add nullable `Icon varchar(100)` and expose it through create, update, read, CSV/XLSX import and CSV export.
- Rename the permission module to `TENANT_POLICY_HOLIDAY`, retain its existing Id and stable UI route/page key, and retain View/Add/Update/Delete/Import/Export plus plan mappings.

## Environment and prerequisites

- Local repository build and Development-configured PostgreSQL database.
- Existing table contained 31 Tenant 8 / Jabalpur 2026 holiday rows.
- A data-only backup was created at `DBFullBACKUP/Holiday-before-rename-20260923.sql` before the migration. Credentials are not recorded.

## API and persistence changes

| Area | Before | After |
| --- | --- | --- |
| API root | `/api/HolidayCalandar` | `/api/Holiday` |
| Table | `axionpro."OrganizationHolidayCalendar"` | `axionpro."Holiday"` |
| Entity / response | `OrganizationHolidayCalendar` / `OrganizationHolidayCalendarDTO` | `Holiday` / `HolidayDTO` |
| Module code / label | `TENANT_POLICY_HOLIDAY_CALENDAR` / Organization Holiday Calendar | `TENANT_POLICY_HOLIDAY` / Holiday |
| New field | none | nullable `Icon varchar(100)` |

The stable Angular route `/app/holidays` and immutable `PageName=tenant-policy-holiday-calendar` were retained. Numeric permission IDs remain runtime data and must be resolved through the authenticated menu/permission flow.

## Verification

| Check | Expected | Actual | Result |
| --- | --- | --- | --- |
| Pre-migration row count | 31 | 31 | PASS |
| Rename migration and rerun | Commit twice without data loss | Committed; rerun idempotent | PASS |
| Old/new table | Old absent, new present | Old absent; `axionpro."Holiday"` present | PASS |
| Icon schema | nullable varchar(100) | character varying(100) | PASS |
| Rows after migration | 31 retained | 31 total; all 31 expected Tenant 8/location 5/year 2026 rows retained | PASS |
| DB object names | Holiday PK/FKs/indexes/sequence | Renamed and verified | PASS |
| Module | existing Id retained, renamed | Id 118, `TENANT_POLICY_HOLIDAY`, label Holiday | PASS |
| Operations | six active operations | View, Add, Update, Delete, Import, Export | PASS |
| Plans | mappings retained | 10 active plan mappings | PASS |
| Seed rerun | idempotent | completed twice without duplicate mapping | PASS |
| Existing 2026 source script rerun | parse 31 Icon-aware rows and insert no duplicates | `COPY 31`, `INSERT 0 0`, commit | PASS |
| Solution build | no errors | 0 errors; pre-existing warnings remain | PASS |
| Focused automated tests | all run and pass | 16 passed, 0 failed, 0 skipped | PASS |

## Automated coverage

- `HolidaySchemaTests`: entity/DTO shape, rename migration and both module seed mirrors.
- `HolidayCrudContractTests`: authenticated `/api/Holiday` CRUD/import/export route and permission contracts, including Icon.
- `HolidayPermissionTests`: module/operation/runtime permission pipeline.
- `HolidayCrudDatabaseTests`: rolled-back PostgreSQL CRUD, tenant scope, inactive conflict, soft-delete release and Icon persistence.
- `HolidayTransferDatabaseTests`: rolled-back import/export, Icon round-trip, duplicate/invalid atomicity.

Command:

```powershell
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter Holiday --verbosity minimal -p:NuGetAudit=false
```

## Deployment status

The Development-configured target DB and local code/tests were verified. No deployed environment or authenticated live HTTP client was tested in this scenario.
