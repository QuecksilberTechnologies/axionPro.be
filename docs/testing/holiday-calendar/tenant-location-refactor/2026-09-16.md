# Holiday calendar: tenant-location refactor — 2026-09-16

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-LOCATION-001`
- Acceptance criteria: replace copied country/state/year values with a required
  tenant-location relationship, use a date-only holiday value, remove duplicate
  remark data, and preserve tenant isolation/status/audit fields.
- Overall status: PASS locally; target migration and deployed acceptance PENDING.
- Environment: local Windows workspace, .NET 10; no deployed API call performed.
- Branch/commit: working tree, commit not created at test time.
- Run time: 2026-09-16, Asia/Calcutta.
- UI handoff: [Organization Holiday Calendar](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md).

## Prerequisites and test data

No production record was created. The test inspects the entity, DTO, EF mapping,
and guarded SQL migration. The target table was known to contain no rows during
the preceding database audit, but that observation was not treated as permission
to alter the live database while an old API build remained deployed.

## Steps and results

| Case | Action | Expected result | Actual result | Environment | Status |
| --- | --- | --- | --- | --- | --- |
| HC-01 | Inspect entity contract | Location FK/date-only present; redundant properties absent | Contract matched | Local | PASS |
| HC-02 | Inspect response DTO | Location, date, optional flag and description exposed | Contract matched | Local | PASS |
| HC-03 | Inspect migration contract | Guard, FK, index, date conversion and redundant-column drops present | Contract matched | Local | PASS |
| HC-04 | Compile affected solution through focused test project | Build succeeds | Build succeeded | Local | PASS |
| HC-05 | Execute migration against target DB | Safe schema transition | Not run; requires coordinated application rollout | Target DB | PENDING |
| HC-06 | Call deployed GET endpoint | New response contract returned | Not run | Deployed | PENDING |

## Investigation and changes

The original row duplicated geography (`CountryCode`, `StateCode`) already owned
by `TenantLocation`, stored a redundant `HolidayYear`, used a timezone timestamp
for a calendar date, and had both `Description` and `Remark`. The refactor uses
`TenantLocationId`, derives the year from `HolidayDate`, stores PostgreSQL `date`,
and retains `Description` as the single descriptive field.

The migration aborts if it must introduce a required `TenantLocationId` while
rows already exist. This prevents arbitrary location assignment. It is not run
against the live target before deployment because dropping mapped columns would
make the currently deployed old build incompatible.

## Automated verification

- Project: `axionpro.automationtests`
- Category: `OrganizationHolidayCalendarRefactor`
- Command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter TestCategory=OrganizationHolidayCalendarRefactor --logger "console;verbosity=minimal"`
- Result: 3 passed / 0 failed / 0 skipped / 3 total.
- Build: PASS as part of the test command.
- Existing NuGet vulnerability warnings were emitted; they did not fail this run.

## Persistence and cleanup

No DB record or file was created. The intended destination remains
`axionpro."OrganizationHolidayCalendar"`, linked to
`axionpro."TenantLocation"`. No cleanup was required.

## Remaining acceptance and completion criteria

1. Coordinate application deployment and target migration so neither old nor new
   code runs against an incompatible schema.
2. Verify the final target columns, FK, index, and date type.
3. Call the deployed GET endpoint and capture its sanitized response.
