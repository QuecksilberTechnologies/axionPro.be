# Holiday calendar: target DB migration — 2026-09-22

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-LOCATION-002`
- Requested change: align the pictured 18-column holiday table with the existing location-based API model.
- Overall status: DB schema and local focused tests PASS; deployed API acceptance PENDING.
- Environment: Windows workspace, Development-configured PostgreSQL database `workforcedb_34hi_duis`, .NET 10. No deployed API URL was tested.
- Branch/commit: current working tree; no commit created here.
- Timezone: Asia/Calcutta; exact run time not retained.
- Earlier [design/test report](2026-09-16.md) and [UI handoff](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md).

## Prerequisites and test data

Before migration, the target table had 18 columns and 0 rows. No employee, tenant, or holiday test record was inserted. No permission-protected API was called, so ModuleId/OperationId were not needed.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| HC-06 | Query row count and original columns | Establish migration safety | 0 rows, 18 original columns | Target DB | PASS | Direct PostgreSQL read-back in task session |
| HC-07 | Back up table with `pg_dump -Fc` | Nonempty restorable table archive | 5,130-byte dump created | Local file | PASS | `C:\AxionProCodeBase\DBFullBACKUP\OrganizationHolidayCalendar_before_refactor.dump` |
| HC-08 | Run `database-scripts/RefactorOrganizationHolidayCalendar.sql` | Transaction commits | Committed; rerun was idempotent | Target DB | PASS | `psql -X -v ON_ERROR_STOP=1 -f database-scripts\RefactorOrganizationHolidayCalendar.sql` |
| HC-09 | Query resulting columns and FK | 15 columns, location FK, date-only date | Confirmed; `TenantLocationId` NOT NULL and FK present | Target DB | PASS | Direct PostgreSQL read-back in task session |
| HC-10 | Focused automated tests | 3 pass | 3 passed, 0 failed, 0 skipped | Local | PASS | Command below |
| HC-11 | Call deployed holiday GET | New response contract | Not run | Deployed | PENDING | No authenticated call |

## Investigation and changes

The API entity, DTO, and guarded migration already existed. This task applied the migration to the configured target database. It removed `CountryCode`, `StateCode`, `HolidayYear`, and `Remark`; added required `TenantLocationId`; changed `HolidayDate` from `timestamptz` to `date`; and created the location FK and date index. No API source code was changed. An old deployed API build could still map the removed columns; that compatibility remains unverified.

## Automated verification

- Project/category: `axionpro.automationtests`, `OrganizationHolidayCalendarRefactor`.
- Command: `dotnet test axionpro.automationtests --filter OrganizationHolidayCalendarRefactor --no-restore --verbosity quiet -p:NuGetAudit=false`
- Result: 3 passed / 0 failed / 0 skipped / 3 total; project compiled as part of the run.
- Existing package advisory warnings appeared and did not fail the tests.
- These tests verify the local contract and migration text; they do not prove deployed API behavior.

## Persistence and cleanup

No holiday data was inserted or deleted because the table was empty. The schema change is retained. The table backup is retained at the path above. No cleanup was performed.

## Remaining acceptance and completion criteria

1. Verify the running API build uses the location-based entity and call the authenticated holiday GET endpoint.
2. Confirm the current UI consumer tolerates the changed response contract. No UI code was changed in this task.
