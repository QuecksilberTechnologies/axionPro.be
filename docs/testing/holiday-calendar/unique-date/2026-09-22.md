# Holiday calendar: one nondeleted entry per date — 2026-09-22

## Scope and status

- Scenario ID: `HOLIDAY-CALENDAR-UNIQUE-DATE-001`.
- Requested behavior: for the same tenant/location/date, block a new holiday even when the existing row is inactive or has a different name; permit a new row only after the old row is soft-deleted; tell the caller to edit or delete the existing entry.
- Result: local code, target DB index, rollback DB checks and 14 focused tests PASS. Authenticated HTTP and deployed build NOT RUN.
- Environment: Windows, .NET 10, Development-configured PostgreSQL `workforcedb_34hi_duis`; current working tree, no commit/deployed build verified. Date 2026-09-22 Asia/Calcutta; exact time not captured.
- UI handoff: [Organization Holiday Calendar](../../../UIdeveloperDoc/ORGANIZATION_HOLIDAY_CALENDAR.md).

## Prerequisites and test data

The affected module is `TENANT_POLICY_HOLIDAY_CALENDAR`. The UI must resolve ModuleId and View/Add/Update/Delete/Import operation IDs from the authenticated permission flow. TenantId comes from authentication. Existing target data included 31 holidays for tenant 8/location 5 in 2026, with no nondeleted duplicate dates. Rollback-only checks used the existing 2026-01-26 row and temporary differently named replacement; no personal data or credentials were recorded.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| HC-UD-01 | Inspect existing nondeleted tenant/location/date groups | No pre-existing duplicates before index | Zero duplicate groups | Target DB | PASS | Direct SQL query |
| HC-UD-02 | Apply [guarded partial unique index](../../../../database-scripts/EnforceOrganizationHolidayCalendarUniqueDate.sql) | Only non-soft-deleted rows constrained | Transaction committed; index created | Target DB | PASS | `psql -X -v ON_ERROR_STOP=1 -f database-scripts/EnforceOrganizationHolidayCalendarUniqueDate.sql` |
| HC-UD-03 | Set existing row inactive, try a differently named same-date insert | Unique violation blocks insert | Caught unique violation in rollback-only SQL block | Target DB | PASS | Transactional SQL smoke |
| HC-UD-04 | Soft-delete same existing row, insert replacement on date | Replacement allowed | Insert succeeded inside rollback-only transaction | Target DB | PASS | Transactional SQL smoke |
| HC-UD-05 | Roll back smoke changes | Original live row intact | One non-soft-deleted row remains on 2026-01-26 | Target DB | PASS | Direct post-rollback count |
| HC-UD-06 | Focused tests | CRUD/import conflict behavior | 14 passed, 0 failed, 0 skipped | Local + rollback DB | PASS | Command below |
| HC-UD-07 | Authenticated create/update/import errors | HTTP 400 with existing Id/status and no write | Not called | API | PENDING | No authenticated session |

No raw authenticated HTTP response was captured. The duplicate JSON in the UI handoff is illustrative. The database smoke deliberately rolled back; it did not delete or modify the 2026 live calendar.

## Investigation and changes

The previous CRUD check matched location/date/**name**, and the import skipped existing active matches. This allowed another name on the same date and did not treat inactive rows consistently. The repository now finds any non-soft-deleted row by tenant/location/date, irrespective of active state or name. Create/update return a validation message with the existing row Id and active/inactive status. Update and delete can target an inactive nondeleted row by Id; detail GET can retrieve it so the UI can edit it. The list still shows active rows. Import rejects any same-date conflict in the file or DB before saving any row, including inactive DB rows. A partial unique index prevents concurrent writes from bypassing the application check; its unique violation is translated to the validation error. Soft-deleted rows do not occupy the date.

## Automated verification

- Build: `dotnet build axionpro.api/axionpro.api.csproj --no-restore -v quiet -p:NuGetAudit=false` — 0 errors; pre-existing warnings remain.
- Test: set `AXIONPRO_HOLIDAY_TEST_CONNECTION` from local Development configuration, then `dotnet test axionpro.automationtests --filter 'HolidayCalendar|OrganizationHolidayCalendarRefactor' --no-restore --verbosity quiet -p:NuGetAudit=false` — 14 passed / 0 failed / 0 skipped / 14 total. The connection string was not recorded and the environment variable was removed afterward.
- `HolidayCalendarCrudDatabaseTests` covers inactive conflict, same-record edit access, and soft-deleted release. `HolidayCalendarTransferDatabaseTests` covers differently named same-date import rejection, inactive rejection, invalid row atomicity and soft-deleted replacement. Test writes were rolled back.
- Direct SQL smoke separately exercised the partial unique index with an inactive existing row and a soft-deleted replacement in one rolled-back transaction.

## Persistence and cleanup

The index `UX_OrganizationHolidayCalendar_Tenant_Location_Date_NotDeleted` remains on `axionpro."OrganizationHolidayCalendar"` in the configured target DB. It keys `TenantId`, `TenantLocationId`, `HolidayDate` where `IsSoftDeleted IS DISTINCT FROM TRUE`. The 31 live rows from the prior calendar task remain; the smoke's status changes and replacement were rolled back. No deployed DB was checked.

## Remaining acceptance and completion criteria

1. With an authorized tenant account, call create and import for an existing date under active and inactive states, confirm HTTP validation response and unchanged row count; edit/delete the existing Id and verify reinsert after soft delete.
2. Verify the migration and API on each deployment target; the index script intentionally fails if historical nondeleted duplicate dates exist, so reconcile those records before applying it.
