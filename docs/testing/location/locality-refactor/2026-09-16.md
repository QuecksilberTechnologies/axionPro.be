# Location: City to Locality refactor — 2026-09-16

## Scope and status

- Scenario ID: `LOCATION-LOCALITY-001`
- Acceptance: data-preserving City rename; District foreign key; City/Town/Village
  type seed; Locality and LocalityType option endpoints.
- Overall status: target DB migration PASS; local API smoke PASS; deployed endpoint verification PENDING.
- Environment: target PostgreSQL plus local API process; deployed API not called successfully in this run.
- UI handoff: [Locality and Locality Type API](../../../UIdeveloperDoc/LOCALITY_AND_LOCALITY_TYPE_API.md)

## Prerequisites and test data

The existing location reset/sync scripts establish a one-to-one City.Id and
District.Id catalogue. The migration verifies both Id and StateId before making
DistrictId non-null and rolls back on any unmapped row. Examples in the UI handoff
are illustrative and were not inserted by this test.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status |
| --- | --- | --- | --- | --- | --- |
| L-01 | Build API project | Compile updated entities, handlers and mappings | 0 errors | local | PASS |
| L-02 | Focused LocalityRefactor tests | Entity, DTO, route and migration contracts pass | 6 passed, 0 failed/skipped | local | PASS |
| L-03 | Apply migration | Preserve rows and seed 3 types | 8,333 rows; 0 missing District/type mappings; City, Town, Village active | target DB | PASS |
| L-04 | Call LocalityType option | Return three seeded values | Success; City, Town, Village | local API with configured DB | PASS |
| L-05 | Call Locality option for DistrictId 1 | Return locality with type context | Success; 1 row; type City | local API with configured DB | PASS |
| L-06 | Call both endpoints on deployed build | Successful real responses | Updated build not deployed in this run | deployed | PENDING |

## Investigation and changes

The existing `SyncDistrictsFromCities.sql` intentionally copied City.Id to
District.Id, so the migration can establish the District FK deterministically.
The migration includes a compatibility City view so the currently deployed build
can continue reading during coordinated rollout.

## Automated verification

- Command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter Category=LocalityRefactor`
- Focused result: 6 passed, 0 failed, 0 skipped, total 6.
- Combined regression (`LocalityRefactor|HostApi|RecentDeviceEmployee`): 167 passed,
  0 failed, 3 skipped, total 170. The three skips require their configured external
  database prerequisites and are not recorded as passes.
- Build: `dotnet build axionpro.api/axionpro.api.csproj --no-restore --verbosity quiet` — PASS, 0 errors.

## Persistence and cleanup

The target migration renamed the 8,333-row catalogue, populated every DistrictId
and LocalityTypeId, seeded LocalityType ids 1–3, backfilled TenantLocation where a
locality existed, and retained the temporary City compatibility view. These are
intentional permanent changes. No files are stored by these endpoints.

## Remaining acceptance and completion criteria

- Deploy the updated API.
- Verify both new lookup endpoints with real authenticated UI/API traffic.
