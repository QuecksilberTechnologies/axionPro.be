# Duplicate Export operation cleanup — 2026-09-23

## Result

**PASS on the Development-configured database.** Duplicate Export Operation Id 14 (`OperationType=4`) was removed. Canonical Export Operation Id 23 (`OperationType=11`) remains active.

## Before

- Two active `Export` rows existed: Id 14 and Id 23.
- Id 14 had two `ModuleOperationMapping`, two `TenantEnabledOperation` and two `RoleModuleAndPermission` rows.
- A pre-change backup was saved at `DBFullBACKUP/DuplicateExport-before-removal-20260923.txt`.

## Migration behavior

`database-scripts/RemoveDuplicateExportOperation.sql` finds the canonical active bulk Export by `OperationType=11`. Before deleting another Export operation, it copies missing module mappings, tenant entitlements and role permissions to the canonical operation. It then deletes old host-role, role, tenant and module-operation dependencies followed by the duplicate Operation row.

This preserved Export for `EMP_LIST` and `TENANT_AUDIT_LOGS` under Operation Id 23.

## Actual database result

| Check | Actual |
| --- | ---: |
| Export rows | 1 |
| Active canonical Id 23 / type 11 | 1 |
| Operation Id 14 rows | 0 |
| Id 14 module mappings | 0 |
| Id 14 tenant operations | 0 |
| Id 14 role permissions | 0 |
| Id 14 host-role permissions | 0 |

## Automated validation

Focused command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~DuplicateExportOperationCleanup" --no-restore --verbosity quiet -p:NuGetAudit=false`

Result: **2 passed, 0 failed, 0 skipped**. Existing package advisory warnings were emitted; no build/test error occurred.

## Status boundary

The local Development-configured DB and seed contracts were tested. Deployed database verification was not run.

## Production-seed correction

After review, duplicate-data repair was removed from every affected production seed. The seeds now select or create only the canonical Export operation (`OperationType=11`); they do not insert or migrate a duplicate and then delete it. The old Add/Create consolidation and Module 38 migration logic were also removed from the consolidated production seed. Those historical corrections remain separate, explicitly run migration scripts.

Policy seed staging tables now enforce source uniqueness with primary keys on `OperationName` and on `(ModuleCode, OperationName)`. EmployeeType seed references only canonical Export and contains no duplicate-Export cleanup block.

Combined seed and configured-DB verification after this correction: **7 passed, 0 failed, 0 skipped**.
