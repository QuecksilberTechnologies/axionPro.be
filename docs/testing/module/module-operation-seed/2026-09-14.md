# Module seed metadata and operation cleanup — 2026-09-14

## Scope

- Authoritative seed: `database-scripts/complete seed data/AxionPro_New_Production_Module_Operation_Seed.sql`.
- Keep one canonical `TENANT_DASHBOARD` navigation-parent seed entry.
- Do not issue database deletes for Dashboard records from this seed.
- Preserve non-empty values and fill empty Module `Remark`, `ImageIconWeb` and
  `ImageIconMobile` values.
- Retain the approved Create-to-Add reference migration and Create removal.

## Local validation

| Scenario | Expected | Current result |
| --- | --- | --- |
| Source contract | Canonical `TENANT_DASHBOARD` is seeded; no Dashboard delete is issued; metadata normalization and Add/Create assertion are present | PASS: 1/1 |
| Affected Host seed regressions | Existing Host seed tests remain green | PASS: 3/3 |
| Isolated PostgreSQL | No empty Module metadata; one active Add and zero Create operations | BLOCKED / NOT RUN: 0 passed, 1 skipped because `AXIONPRO_BULK_TEST_CONNECTION` was unavailable |

Commands:

```powershell
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "Category=ModuleOperationSeed|FullyQualifiedName~Host_seed"
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --no-restore --filter "Category=ModuleOperationSeedDatabase"
```

Database validation must not be reported as passed until the complete seed is run
twice against the disposable PostgreSQL fixture and the database-category test passes.
