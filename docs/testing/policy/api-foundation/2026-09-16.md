# Policy API foundation scenario — 2026-09-16

## Environment and scope

- Environment: local source/build; no deployed API claim.
- Database schema prerequisite: `CreateGenericTenantPolicyFramework.sql` was
  previously applied and verified in the target DB.
- Scope: EF mapping, authenticated permission contract, policy type/policy draft,
  lifecycle, approval stages, resolution, assignment, exception and acknowledgement APIs.

## Expected and actual

Expected: all 16 policy entities map to the existing PostgreSQL schema; every API
request uses dynamic ModuleId/OperationId; tenant identity comes from the token;
draft creation/update is transactional; only documented lifecycle transitions are
accepted; employee operations cannot cross tenant boundaries.

Actual: local solution build passed with zero errors. Focused automated results are
recorded below after execution. No real employee/policy rows were inserted by this
local contract run. No production endpoint was called.

## API sequence covered

1. Lookups and policy type CRUD/status.
2. Policy list/detail and transactional draft create/update.
3. Version clone and submit/approve/reject/publish/archive.
4. Resolve effective policies for an employee.
5. Approval-stage CRUD, role/minimum-approval enforcement and progress reads.
6. Manual assign/remove, assignment list, exception submission/decision/list and
   acknowledgement/list. Assignment creates missing acknowledgement evidence.
7. Document upload/list/delete and policy audit listing.

## Persistence and cleanup

The implementation writes to `PolicyType`, `Policy`, `PolicyVersion`, `PolicyRule`,
`PolicyApplicability`, `PolicyAssignment`, `PolicyException`,
`PolicyAcknowledgement` and `PolicyChangeAudit`. This report's tests are model and
contract tests, so cleanup was unnecessary.

## Pending acceptance

- Authenticated deployed endpoint flow: NOT RUN.
- Document object-storage integration: IMPLEMENTED locally; live storage call NOT RUN.
- Approval-stage administration and enforcement: IMPLEMENTED locally; DB-backed
  and deployed authenticated execution NOT RUN.
- Durable policy bulk preview/confirm/worker/report: IMPLEMENTED LOCALLY after
  this foundation run; see `../bulk-import/2026-09-16.md` for current evidence.
- PostgreSQL integration tests: BLOCKED/NOT RUN because neither
  `AXIONPRO_BULK_TEST_CONNECTION` nor `ConnectionStrings__DefaultConnection` is
  configured in this test process. This is not counted as a pass.

## Automated validation

- Command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "FullyQualifiedName~PolicyFrameworkSchemaTests|FullyQualifiedName~GenericPolicyApiContractTests" --logger "console;verbosity=minimal"`
- Final result after approval stages, operational lists, geography validation,
  deterministic precedence and assignment acknowledgement behavior:
  **12 passed, 0 failed, 0 skipped**.
- API/persistence builds: **0 compile errors**. Existing repository/package warnings
  remain outside this feature's scope.
- Broader suite run: **465 passed, 89 skipped, 5 failed**. Two source-contract
  failures were caused by the intentional restoration of `PolicyType` and an
  incorrect SQL delimiter-count assertion; both tests were corrected and their
  focused regression run passed **14/14**. The remaining three failures require a
  running API at `localhost:5170` (navigation, client-info and Swagger) and failed
  with `ECONNREFUSED`; they are environment-blocked and are not recorded as passes.
