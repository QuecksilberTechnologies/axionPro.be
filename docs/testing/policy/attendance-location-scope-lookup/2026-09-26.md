# Policy: Attendance location-scope lookup — 2026-09-26

## Scope and status

- Scenario ID: POLICY-ATTENDANCE-SCOPE-LOOKUP-001
- User-requested behavior / acceptance criteria: Policy Definition attendance location-scope options must come from the authenticated API so UI numeric mappings cannot drift from the backend enum.
- Overall status: PASS for local source/build verification; authenticated browser and deployed acceptance remain pending.
- Environment and API base URL: local source/build; no deployed API call was made.
- Branch, code commit and independently verified deployed build: backend `main_branch`; UI latest `development` at `598d1572` with the uncommitted feature changes applied; deployment not verified.
- Run time and timezone: 2026-09-26, Asia/Calcutta.
- Prior report / related UI handoff: [Tenant Policy endpoint catalog](../../../UIdeveloperDoc/TENANT_POLICY_ENDPOINT_CATALOG.md)

## Prerequisites and test data

The endpoint requires an authenticated Tenant user and dynamically resolved Policy Definitions `View` ModuleId/OperationId. No credentials, Tenant data or database writes are required for the enum-derived lookup contract.

Illustrative query:

```http
GET /api/TenantPolicy/lookups?moduleId=<resolved-policy-definitions-id>&operationId=<resolved-view-id>
Authorization: Bearer <redacted>
```

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| SCOPE-01 | Build and focused Policy contract tests | Backend compiles; all four enum values are covered | 16 passed, 0 failed, 0 skipped | Local | PASS | `GenericPolicyApiContractTests` command below |
| SCOPE-02 | Angular production build | Typed additive response and Policy form integration compile | Build completed successfully; existing bundle-budget warnings only | Local | PASS | `bun run build` |
| SCOPE-02A | Focused Policy form schema test | Missing API scope blocks Attendance save; non-Attendance policies remain unaffected | 4 passed, 0 failed | Local | PASS | Focused Angular command below |
| SCOPE-03 | Open Create Policy with Attendance type | Location scope is populated from `attendanceLocationScopes`; recommended default is selected by code | Not run in an authenticated browser session | Local | PENDING | Requires authenticated UI smoke |
| SCOPE-04 | Call deployed lookup endpoint | Authenticated response contains the four backend-derived options | Not run | Deployed | PENDING | Deployment required |

## Investigation and changes

- Observed failure: the Policy Definition form imported a local `ATTENDANCE_LOCATION_SCOPES` numeric array.
- Confirmed cause and evidence: the select in `policy-definition-form.ts` used that array instead of the existing `GET /api/TenantPolicy/lookups` response.
- Suspected cause / remaining uncertainty: none for the source-level mapping; authenticated browser and deployed response remain unverified.
- Changed files and commit: backend lookup DTO/handler/controller documentation/test; UI lookup interface/store/dialog/form; endpoint catalog. No commit was created in this run.
- What the executed regression tests establish and do not establish: the backend compiles, its contract suite passes, the UI production bundle compiles, and the focused form schema handles missing scope data. These local checks do not establish authenticated runtime or deployed acceptance.

## Automated verification

- Backend command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~GenericPolicyApiContractTests" --no-restore`
- UI command: `bun run build`
- Focused UI command: `bunx ng test --watch=false --include="src/app/features/tenant-policies/policy-definitions/policy-definition-form/policy-definition-form.schema.spec.ts"`
- Counts: backend 16 passed / 0 failed / 0 skipped; focused UI 4 passed / 0 failed; Angular production build passed.
- Environment prerequisites and skip reasons: no database is needed for the focused source/contract test; authenticated HTTP was not available in this run.
- Sanitized output location or recorded summary: terminal output only.
- Build result: backend and UI builds passed. The UI build reported pre-existing initial bundle and editor CSS budget warnings.

## Persistence and cleanup

No database records or files outside the source/documentation trees are created by this lookup change. No cleanup is required.

## Remaining acceptance and completion criteria

- Run an authenticated local browser/API smoke and confirm all four options render and submit unchanged.
- Deploy both coordinated backend and UI builds before deployed acceptance.
