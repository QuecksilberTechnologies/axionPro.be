# Tenant Policy deployed route smoke — 2026-09-16

## Environment/build

- Target: `https://axionpro-api.onrender.com`
- Render service: `axionpro-api`
- Live commit observed in Render: `ddeb4d7e`
- Authentication: deliberately omitted for route/authentication smoke

## Prerequisites and sequence

1. Read deployed `/swagger/v1/swagger.json` with a cache-busting query.
2. Select every `/api/TenantPolicy` HTTP operation.
3. Replace path placeholders with syntactically valid sample values.
4. Call each operation without Authorization and record only the status.
5. Do not send valid mutation data; this run must create no records.

## Expected and actual

- Expected: all 37 operations exist. JSON routes reach authorization and return
  401. Multipart routes may reject deliberately incorrect JSON as 415 before the
  action executes.
- Actual: Swagger exposed 37 operations. 35 returned 401. `POST /documents` and
  `POST /bulk/{target}/preview` returned 415 when sent `application/json` instead
  of multipart form data. No endpoint returned 404 after deployment propagation.
- Result: PASS for deployment/route/content-type registration only.

## Persistence and cleanup

No authenticated action executed and no policy/bulk record was created. No
cleanup was required.

## Automated contract evidence

`GenericPolicyApiContractTests` passed 14/14 with zero failures/skips after adding
the endpoint-catalog coverage assertion. It verifies that the controller exposes
exactly 37 operations and the catalog contains a numbered entry for each one.

## Pending

- BLOCKED: authenticated endpoint-by-endpoint business-flow testing needs a valid
  tenant JWT with the seeded policy leaf-module permissions.
- NOT RUN: target DB policy bulk migration verification, bulk worker completion,
  report/database reconciliation and document object-storage round trip.
- Sample contracts for all endpoints:
  `docs/UIdeveloperDoc/TENANT_POLICY_ENDPOINT_CATALOG.md`.
