# Policy framework schema and module seed — 2026-09-16

## Scope and status

- Scenario ID: `POLICY-FRAMEWORK-001`
- Scope: generic policy persistence, master seed, module hierarchy, operations and plan mapping.
- Environment: target PostgreSQL and local automated contract suite.
- Controllers/endpoints/UI: deliberately not part of this phase.

## Expected versus actual

| Case | Expected | Actual | Environment | Status |
| --- | --- | --- | --- | --- |
| PF-01 | Create 16 generic policy tables transactionally | 16 tables returned by migration verification query | target DB | PASS |
| PF-02 | Seed categories, statuses, rule types and document types | Seed statements completed in same committed transaction | target DB | PASS |
| PF-03 | Create one scope-1 parent and seven child modules | Parent and seven leaf modules returned with correct hierarchy/PageName | target DB | PASS |
| PF-04 | Reuse operations and add missing lifecycle actions without duplicates | Existing actions reused; Publish, Archive and Acknowledge added by normalized-name guard | target DB | PASS |
| PF-05 | Map policy modules to active plans | PlanModuleMapping seed completed | target DB | PASS |
| PF-06 | Invalid seed column casing must not partially save | First module seed attempt failed on PageUrl; enclosing transaction rolled back | target DB | PASS |
| PF-07 | Correct actual PageURL/IconURL schema and rerun | Corrected seed completed and returned operation matrix | target DB | PASS |
| PF-08 | Rerun schema and module seed | Same 16 tables, 8 unique modules, 47 active operation mappings | target DB | PASS |
| PF-09 | Verify operation duplication | Publish, Archive and Acknowledge each have exactly one normalized-name row | target DB | PASS |
| PF-10 | Verify active-plan mappings | 8 policy modules mapped across 10 active plans | target DB; 80 active mappings | PASS |
| PF-11 | Focused automated contracts | Schema, module, operation and consolidated-seed contracts | local; 4 passed, 0 failed/skipped | PASS |
| PF-12 | Reconcile master and FK counts | 12 categories, 7 statuses, 13 rule types, 5 document types and 46 policy FKs | target DB | PASS |

## Persistence and evidence

Migration: `database-scripts/CreateGenericTenantPolicyFramework.sql`.
Module seed: `database-scripts/complete seed data/SeedTenantPolicyModules.sql`.
The same idempotent block is mirrored inside the consolidated production module seed.
Design reference: `docs/POLICY_FRAMEWORK_DESIGN.md`.
UI evaluation: `docs/UIdeveloperDoc/TENANT_POLICY_UI_PLANNING.md`.

No sample employee policy was inserted. Master catalogue and permission metadata
are intentional permanent records. The first failed module-seed execution was
transactionally rolled back and is not recorded as a pass.

## Remaining work

- Implement controllers/endpoints only in the separately approved next phase.
- Run entitlement sync for a tenant before expecting menu/role permission rows.
