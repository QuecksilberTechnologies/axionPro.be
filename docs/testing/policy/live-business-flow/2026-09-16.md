# Tenant policy: deployed real-data business flow — 2026-09-16

## Scope and status

- Scenario ID: `POLICY-LIVE-E2E-20260916`
- Acceptance: exercise the deployed policy APIs with current tenant master and
  employee rows, then reconcile persisted results in the target PostgreSQL DB.
- Overall status: **PARTIAL PASS / one external dependency BLOCKED**.
- Environment: `https://axionpro-api.onrender.com`, Render PostgreSQL
  `workforcedb_34hi_duis`, Asia/Calcutta.
- Branch: `main_branch`. The deployed API behavior includes the route-ID fix from
  `64eec6be`; the Render dashboard commit label was not retained as evidence.
- UI handoff: [Tenant Policy endpoint catalogue](../../../UIdeveloperDoc/TENANT_POLICY_ENDPOINT_CATALOG.md).

## Prerequisites and sanitized real data

The authenticated Tenant Admin belonged to tenant ID `8`, role ID `22`, and
employee ID `4`. IDs were read from the authenticated context/target DB; secrets
and tokens are omitted. Module and operation IDs were resolved from `my-menu`.

- Policy type: existing active `BQA20260916195752`, ID `6`.
- Policy: `LIVEPOL20260916201836`, ID `3`.
- Version 1: ID `3`; Version 2 clone: ID `4`.
- Real employees: ID `4` (manual assignment) and ID `6`, employee code
  `QT/2026/0145` (bulk assignment).
- Applicability used country ID `1`, employee type ID `7`, priority `500`.
- Rule JSON enabled device, mobile and web attendance and carried an annual-day
  entitlement. JSON was stored and read back in normalized form.

## Steps and results

| Case | Action | Expected | Actual | Environment | Status |
| --- | --- | --- | --- | --- | --- |
| P01 | Login, `my-menu`, lookups and types | Tenant context and dynamic permissions resolve | 8 policy modules; lookups/types returned | Render | PASS |
| P02 | Policy Type create/update/inactivate/activate | Route IDs bind; all mutations persist | Created ID `8`, updated currency `USD`, disabled then enabled | Render | PASS |
| P03 | Create/list/detail/update policy draft | Version 1 stores rule and applicability | Policy `3`, version `3`; read-back matched | Render | PASS |
| P04 | Create two mandatory approval stages | Ordered stages are available | Stage IDs `1` and `2` returned in progress | Render | PASS |
| P05 | Submit and approve ordered stages | Under Review until every stage completes | First approval completed stage 1; second completed stage 2 | Render | PASS |
| P06 | Publish | Approved version becomes Published/current | Version 1 status ID `4`, `IsCurrent=true` | Render + DB | PASS |
| P07 | Manual assignment and list | Employee 4 assigned once | Assignment ID `1`, source `1`, active | Render + DB | PASS |
| P08 | Resolve effective policy | Manual assignment wins scope resolution | Published policy returned for employee 4 | Render | PASS |
| P09 | Exception create/approve/list | Approved override is retained | Exception ID `1`, employee `4`, approval status `3` | Render + DB | PASS |
| P10 | Acknowledge/list | Assigned employee acknowledges | Employee `4`, acknowledgement status `3` | Render + DB | PASS |
| P11 | Audit | Mutations appear in audit | Eight audit entries at captured read | Render | PASS |
| P12 | Clone version | New Draft version preserves rules/scopes | Version 2, ID `4`, Draft | Render + DB | PASS |
| P13 | Reject → resubmit → approve clone | Rejected version may be corrected/resubmitted | `Rejected` → `Under Review` → `Approved` | Render | PASS |
| P14 | Assignment bulk | Employee 6 assigned by durable worker | Assignment ID `2`, source `2`, active; acknowledgement pending | Render + DB | PASS |
| P15 | Invalid employee assignment | Cross/unknown employee blocked | Validation error observed | Render | PASS |
| P16 | Policy document upload on Draft | File reaches object storage and metadata persists | HTTP 500; no metadata row created | Render | BLOCKED |

## Representative inputs

Draft request excerpt:

```json
{
  "policyTypeId": 6,
  "policyCode": "LIVEPOL20260916201836",
  "effectiveFrom": "2026-09-16",
  "rules": [{
    "policyRuleTypeId": 3,
    "ruleName": "Hybrid attendance",
    "ruleOrder": 1,
    "ruleConfiguration": "{\"annualDays\":12,\"allowWeb\":true,\"allowMobile\":true,\"allowDevice\":true}"
  }],
  "applicability": [{
    "applicabilityMode": 1,
    "countryId": 1,
    "employeeTypeId": 7,
    "priority": 500,
    "effectiveFrom": "2026-09-16"
  }]
}
```

Assignment CSV:

```csv
PolicyCode,VersionNumber,EmployeeCode,EffectiveFrom,EffectiveTo,IsMandatory
LIVEPOL20260916201836,1,QT/2026/0145,2026-09-16,,true
```

## Investigation and changes

The upload failure was reproduced on Draft version `4` with a valid `.pdf`.
Render application logs showed `Amazon.S3.AmazonS3Exception: The AWS Access Key
Id you provided does not exist in our records.` The request fails in object
storage before `PolicyDocument` metadata is inserted. This is a Render S3
credential/configuration issue, not a policy lifecycle or file-shape failure.

A separate live control found that cancelling a terminal bulk job returned a
successful no-op although the published endpoint contract says terminal jobs
cannot be cancelled. `BulkImportRepository` now throws `ConflictException` for
terminal cancellation, with an isolated-database regression test added. This
fix is local and requires deployment before live acceptance.

## Automated verification

```text
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "FullyQualifiedName~GenericPolicyApiContractTests|FullyQualifiedName~PolicyFrameworkSchemaTests|FullyQualifiedName~DurableBulkImportDatabaseTests.Completed_job_cannot_be_cancelled" --logger "console;verbosity=minimal"
```

Result: **19 passed, 0 failed, 1 skipped, 20 total**. The new database test was
skipped because `AXIONPRO_BULK_TEST_CONNECTION` was not configured for the
required isolated `axionpro_bulk_test` clone. Compilation passed. Existing
package/compiler warnings remain.

## Persistence and cleanup

Direct target DB reconciliation confirmed `Policy`, `PolicyVersion`,
`PolicyAssignment`, `PolicyException`, and `PolicyAcknowledgement` rows. Version
1 is Published/current; version 2 is Approved and not current. Assignments for
employees 4 and 6 remain as QA evidence. Approval stages created by this run
were disabled. Document metadata was not inserted because S3 rejected upload.

## Remaining acceptance

1. Correct Render S3 access key/secret/bucket configuration, then repeat
   upload → list → delete on Draft version 4.
2. Deploy the terminal-cancel conflict fix and verify completed-job cancel
   returns HTTP 409.
3. A worker-created failed-row job is required to exercise successful Retry;
   retry on a Completed job correctly returned conflict.

