# Policy: resolve with encoded employee ID — 2026-09-26

## Scope and status

- Scenario ID: POLICY-RESOLVE-ENCODED-EMPLOYEE-ID
- User-requested behavior / acceptance criteria: Resolve must accept the same globally salted encoded string returned by Employee list/view APIs, without using `TenantEncriptionKey`.
- Overall status: PASS locally; authenticated HTTP and deployed acceptance pending.
- Environment and API base URL: local source/build and automated tests; no authenticated HTTP request was run.
- Branch: backend `main_branch`; UI `development`.
- Run date and timezone: 2026-09-26, Asia/Calcutta.
- Related UI handoff: [Tenant Policy API](../../../UIdeveloperDoc/TENANT_POLICY_API.md#resolve-employee-identifier-contract).

## Prerequisites and test data

The Employee option API must return an encoded string identifier. The Resolve UI
passes this value unchanged. `ModuleId` and `OperationId` continue to come from
the authenticated menu/permission pipeline. No credentials or live employee data
were retained for this local verification.

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| R-01 | Inspect Employee projection and `IdEncoderService` | Employee IDs use the existing encoder and its global salt | Confirmed in source; the long-ID encoder/decoder both select `GlobalSalt` | local source | PASS | `ProjectionHelper.cs`, `IdEncoderService.cs` |
| R-02 | Inspect Resolve DTO and handler contract | Query accepts encoded string and decodes before repository access | DTO uses string; decoded numeric value is JSON-ignored and repository-only | local source/test | PASS | `GenericPolicyApiContractTests.Resolve_employee_identifier_uses_the_encoded_public_contract` |
| R-03 | Resolve dialog submit | Encoded Employee option value is sent unchanged | Numeric conversion and numeric-only filtering removed | Angular focused tests | PASS | 4 focused tests passed |
| R-04 | Authenticated `GET /api/TenantPolicy/resolve` with real encoded employee | HTTP 200 and correct effective policies | Not run; authenticated tenant session was not available in this execution | local HTTP | PENDING | No response captured |

## Investigation and changes

- Observed failure: Swagger exposed `EmployeeId` as `integer(int64)`, while Employee list/view returns a non-numeric encoded string.
- Confirmed cause: Resolve DTO used `long` and the Angular dialog attempted `Number(employeeId)`.
- Confirmed encoding behavior: `IdEncoderService.EncodeId_long` and `DecodeId_long` use the application's global static salt. The supplied tenant-key argument does not participate in the current long-ID encoding implementation.
- Change: Resolve now accepts and sanitizes the encoded string, decodes it in the handler, and queries by the internal numeric ID plus authenticated tenant ID. Angular sends the selected encoded value unchanged.
- Isolation: repository lookup retains `TenantId` filtering, so a decoded identifier cannot resolve an employee from another tenant.

## Automated verification

- Backend command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter FullyQualifiedName~GenericPolicyApiContractTests`
- Backend result: 17 passed, 0 failed, 0 skipped, total 17.
- Angular command: `npx ng test --watch=false --include="src/app/core/services/tenant-policy-api.spec.ts" --include="src/app/features/tenant-policies/policy-definitions/policy-definition-form/policy-definition-form.schema.spec.ts"`
- Angular result: 5 passed, 0 failed. The service contract test verifies that the encoded employee ID reaches the Resolve query unchanged.
- Angular production build: `npm run build` passed; output generated under `dist/axionpro`.
- Compiler output contained pre-existing warnings; the focused test commands completed successfully.

## Persistence and cleanup

Resolve is read-only. No database record was inserted, updated, or deleted by this
verification. No cleanup was required.

## Remaining acceptance and completion criteria

Run an authenticated request against the intended deployed build using a sanitized
encoded Employee ID returned by that environment's Employee API, then verify the
HTTP response and tenant isolation. This pending check is not recorded as a pass.
