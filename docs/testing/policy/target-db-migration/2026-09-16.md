# Tenant policy target DB migration — 2026-09-16

## Environment and build

- Environment: Render PostgreSQL target used by the deployed API.
- API deployment observed before migration: commit `ddeb4d7e`.
- Secrets, access tokens and connection strings are omitted.

## Scenario

Apply the prerequisite location migration, generic policy persistence, durable
bulk base/range migration, and tenant policy module seed. Every script is
transactional and rerun-safe.

## Expected versus actual

| Check | Expected | Actual |
| --- | --- | --- |
| Legacy city rows | Preserve every row in the Locality hierarchy | Six unmatched rows retained under one explicit `Unassigned / Legacy` district per state. |
| Policy business tables | 11 | 11 |
| Policy navigation modules | one root plus seven leaves | PASS |
| Active leaf operation mappings | 47 | 47 |
| Durable master constraint | values 1 through 14 | PASS |
| Policy Type master-12 insert probe | accepted | PASS, transaction rolled back |

## Defects fixed

1. `RenameCityToLocality.sql` stopped when a legacy state had cities but no
   synchronized district. It now creates one clearly labelled holding district
   per affected state and advances the identity sequence before insertion. It
   does not guess a real geographic district.
2. `SeedTenantPolicyModules.sql` assumed `Module.PageName` existed. The standalone
   seed now adds the nullable column before upserting stable page keys.

## Validation

```text
dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-build --no-restore --filter "FullyQualifiedName~GenericPolicyApiContractTests|FullyQualifiedName~PolicyFrameworkSchemaTests|FullyQualifiedName~LocalityRefactorTests"
```

Actual: 29 passed, 0 failed, 0 skipped.

## Persistence and cleanup

The migrations preserve existing location data and add policy/module records.
The master-12 insert probe ran inside a transaction and was rolled back. Holding
district rows remain for later administrative classification.

## Status

Target migration: **PASS**. Broader lifecycle and remaining bulk targets are
tracked as separate acceptance scenarios.
