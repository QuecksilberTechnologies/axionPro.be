# Tenant Email Template: CRUD and seed — 2026-09-24

## Scope and status

- Scenario: tenant-scoped EmailTemplate copy, module metadata, permission behavior and CRUD.
- Overall status: implementation/build and configured target seed PASS; authenticated HTTP acceptance PENDING.
- UI handoff: [Tenant Email Template API](../../../UIdeveloperDoc/TENANT_EMAIL_TEMPLATE_API.md)

## Steps and results

| Case | Expected | Actual | Environment | Status |
| --- | --- | --- | --- | --- |
| Module hierarchy | `TENANT_EMAIL_TEMPLATE` is child of Module Id 46 | Module Id 119, ParentModuleId 46 | Configured target DB | PASS |
| Operations | Add, Update, Delete, View mappings | 4 mappings | Configured target DB | PASS |
| Plan mapping | Child inherits active parent plans | 1 active mapping | Configured target DB | PASS |
| Tenant entitlements | Enabled module and operations follow enabled parent | 1 module and 4 operations | Configured target DB | PASS |
| Initial data | Every EmailTemplate copied once per Tenant | 10 actual / 10 expected | Configured target DB | PASS |
| Schema | Tenant FK and copied scalar columns | FK verified; build model compiles | Configured target/local | PASS |
| Template code validation | Only existing constant-backed template codes are accepted | Automated constant tests added | Local | PASS |
| Delivery resolution | Tenant SMTP uses active Tenant template and falls back by the same code; Host SMTP uses Host template | Repository/service contract tests added | Local | PASS |
| Authenticated CRUD | Tenant isolation and permission result verified over HTTP | Not run | Deployed | PENDING |
| Idempotent seed rerun | Counts remain unchanged | 10 templates, 4 operation mappings and 1 plan mapping before/after | Configured target DB | PASS |

## Automated verification

API build completed with 0 errors; pre-existing repository warnings remain. `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "TestCategory=TenantEmailTemplate|Name~Resend_email_service|Name~Email_service_resolves_tenant_template" --nologo --verbosity:minimal` passed 15/15 tests with 0 failures and 0 skips. These include direct resolution checks for Tenant template selection, same-code Host fallback, and Host-only selection.

The full automation project was also run: 503 passed, 95 skipped and 5 failed. Two failures are existing unrelated contract expectations (`GetAttendancePolicyOptionsQuery` permission characterization and the EmployeeType seed text contract). Three HTTP tests failed because no API was listening on `localhost:5170`. None of these failures occurred in the Tenant email-template focused suite. Authenticated deployed HTTP and real SMTP delivery remain pending.

## Persistence and cleanup

The idempotent `database-scripts/SeedTenantEmailTemplate.sql` was applied transactionally. Seed records are intentional and retained. The same block is included in the production module-operation seed file. No credentials or tokens are stored in this report.

## Remaining acceptance

Deploy the matching API build, resolve dynamic permission IDs from the authenticated menu, then exercise create/list/read/update/status/delete for two separate Tenants to confirm HTTP-level isolation.
