# Tenant email queue runtime resolution — 2026-09-24

## Scope and status

- Scenario: safely claim queued email and resolve Tenant/default SMTP and template at send time.
- Overall status: local build, focused automated contracts and configured DB schema verification PASS; live SMTP acceptance PENDING.
- UI/API handoff: [Tenant Email Template API](../../../UIdeveloperDoc/TENANT_EMAIL_TEMPLATE_API.md)

## Expected and actual behavior

| Case | Expected | Actual | Status |
| --- | --- | --- | --- |
| Tenant SMTP and active Tenant template | Tenant SMTP and Tenant content | Runtime resolver uses Tenant source | PASS |
| Tenant SMTP, Tenant template missing/inactive | Tenant SMTP and same-code default content | Host template fallback retained | PASS |
| Tenant SMTP unavailable | Default SMTP and same-code default content | Host configuration selects Host template | PASS |
| Template missing everywhere | Queue retry increments and error is retained | Worker marks failed | PASS (local contract/build) |
| Invalid placeholder JSON or unexpected item error | Release processing lease, retain error and increment retry | Worker records the claimed item failure immediately | PASS (local contract/build) |
| Parallel workers | One worker claims a row | PostgreSQL `FOR UPDATE SKIP LOCKED` plus processing lease | PASS (code/build); DB concurrency PENDING |
| Existing queue safety before worker deployment | No old unsent records should be released unexpectedly | Configured DB contains 0 queue rows | PASS (configured DB observation) |

## Verification

`dotnet build axionpro.api/axionpro.api.csproj --no-restore --nologo --verbosity:minimal` completed with 0 errors. `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --no-restore --filter "TestCategory=TenantEmailTemplate|Name~Resend_email_service|Name~Email_service_resolves_tenant_template" --nologo --verbosity:minimal` passed 18/18 with 0 failures and 0 skips. Existing repository/package warnings remain.

## Persistence

`database-scripts/AlterEmailQueueForTenantTemplateResolution.sql` adds `TenantId`, `TemplateCode`, placeholder JSON and processing-lease columns, backfills existing template codes from the central FK, and adds the pending-work index. The idempotent script was applied to the database configured by both Development and Production settings. All 5 required columns were verified. The configured table contained 0 existing rows, so enabling the worker cannot release historical unsent messages.

Live SMTP delivery was not triggered during this verification because that would send an external email. Database concurrency under two simultaneously running API instances remains pending deployment-level verification.
