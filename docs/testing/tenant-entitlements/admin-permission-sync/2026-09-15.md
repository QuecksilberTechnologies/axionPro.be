# Tenant entitlements: Tenant Admin permission synchronization — 2026-09-15

## Scope and status

- Scenario ID: `TENANT-ENTITLEMENT-ADMIN-PERMISSION-SYNC`
- User-requested behavior / acceptance criteria: After Host-triggered plan entitlement synchronization, add only missing enabled module-operation permissions for the active Tenant Admin role and never insert duplicate permission rows.
- Overall status: WIP — local build and wiring contract pass; disposable PostgreSQL behavior test skipped because its required environment was unavailable.
- Environment and API base URL: Local source/build; deployed API was not tested.
- Branch, code commit and independently verified deployed build: Current working tree; not committed or deployed in this run.
- Run time and timezone: 2026-09-15, Asia/Calcutta.
- Prior report / related UI handoff: [SQL rules](../../../sqlRule.md).

## Prerequisites and test data

The full persistence test requires `AXIONPRO_BULK_TEST_CONNECTION` to reference a local disposable PostgreSQL database named `axionpro_bulk_test`. The test refuses non-local hosts and any other database name. It selects an active, non-soft-deleted, non-system-default Tenant Admin having at least one enabled `TenantEnabledOperation`, runs inside a transaction, and rolls the transaction back.

No credentials, tokens, personal data, or deployed environment records were captured.

## Steps and results

| Case | Action / method / endpoint | Expected result | Actual result / HTTP status | Environment | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| TEAP-01 | Build automation-test project | Updated API, application, persistence and tests compile | Build succeeded with 0 errors | Local | PASS | `dotnet build axionpro.automationtests/axionpro.automationtests.csproj --no-restore --verbosity:quiet` |
| TEAP-02 | Inspect synchronization command and repository contract | Entitlement snapshot is saved before permission sync; enabled operations, Tenant Admin role, and missing tuple filters are present | All assertions passed | Local | PASS | `Entitlement_command_persists_snapshot_before_missing_admin_permission_sync` |
| TEAP-03 | Execute missing/duplicate persistence behavior | First run inserts missing tuple once; second run inserts zero; matching row count remains one | Test runner skipped because `AXIONPRO_BULK_TEST_CONNECTION` was unavailable | Local disposable DB | SKIPPED | `Missing_admin_permissions_are_added_once_and_existing_rows_are_unchanged` |
| TEAP-04 | Call deployed Host entitlement API and reconcile DB | Missing Tenant Admin permissions created without duplicates | Not run | Deployed | PENDING | Requires deployment and authorized live request |

## Investigation and changes

- Observed failure: Host entitlement synchronization populated tenant entitlement snapshots but did not backfill the Tenant Admin's missing `RoleModuleAndPermission` rows.
- Confirmed cause and evidence: `SynchronizeTenantPlanEntitlementsCommand` called only `SynchronizeMissingActivePlanEntitlementsAsync`; no Tenant Admin permission synchronization followed it.
- Suspected cause / remaining uncertainty: Deployed behavior remains unverified until the change is deployed and the Host API is invoked.
- Changed files and commit: Command handler, repository interface/implementation, focused tests, and `docs/sqlRule.md`; no commit created in this run.
- What the executed regression tests establish and do not establish: Compilation and source wiring/order are confirmed. PostgreSQL insertion and duplicate behavior were not executed because the disposable test database variable was absent.

## Automated verification

- Test project and command: `dotnet test axionpro.automationtests/axionpro.automationtests.csproj --filter "FullyQualifiedName~TenantAdminEntitlementPermissionSyncDatabaseTests" --logger "console;verbosity=minimal"`
- Tests: `Entitlement_command_persists_snapshot_before_missing_admin_permission_sync`; `Missing_admin_permissions_are_added_once_and_existing_rows_are_unchanged`.
- Counts: 1 passed / 0 failed / 1 skipped / 2 total.
- Environment prerequisites and skip reasons: Database behavior test requires the guarded disposable local PostgreSQL fixture described above.
- Build result: PASS, 0 errors. Existing package and nullable warnings remain outside this scenario's scope.

## Persistence and cleanup

No database records were changed in this run. The skipped persistence test is designed to modify `RoleModuleAndPermission` only inside a rolled-back transaction.

The implemented runtime flow stages missing grants with `HasAccess = true`, `IsActive = true`, and `IsSoftDeleted = false`. Existing exact `RoleId + ModuleId + OperationId` rows are retained unchanged, including inactive or soft-deleted records, so another row is not created.

## Remaining acceptance and completion criteria

1. Run the guarded database test against `axionpro_bulk_test` and record a pass.
2. Deploy the change.
3. Invoke the authenticated Host entitlement synchronization API for an authorized tenant with a known missing Tenant Admin grant.
4. Reconcile the resulting `RoleModuleAndPermission` rows and verify that a second invocation adds no duplicate.
